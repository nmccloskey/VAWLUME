{
  "metadata_version": "0.5.0",
  "schema_version": "0.10-draft",
  "coverage": {
    "complete_domains": [
      { "domain": "identity_config_provenance", "object_count": 10 },
      { "domain": "entities_experiment", "object_count": 4 },
      { "domain": "recordings_geometry", "object_count": 7 },
      { "domain": "extractors_features", "object_count": 13 },
      { "domain": "detections_curation", "object_count": 7 },
      { "domain": "matching_agreement", "object_count": 16 },
      { "domain": "alignment_external", "object_count": 15 }
    ]
  },
  "objects": {
    "schema_info": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "One row per schema version applied to this database, recording which version of the relational model the file carries and when it was installed.",
      "columns": {
        "schema_version": {
          "description": "Version label of the applied schema. Agrees with the header of schema/schema.sql, this table's seeded row, and PRAGMA user_version; a check enforces that all three match."
        },
        "applied_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME applied this schema version to the database. An audit time, not an observation time from the research data."
        },
        "description": {
          "description": "Free-text note shipped alongside the schema version seed, summarizing what changed in that version."
        }
      }
    },
    "projects": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "A research project: the outermost scope that owns recordings, experimental entities, source files, and the configuration profiles under which their source material is interpreted.",
      "columns": {
        "project_id": {
          "description": "VAWLUME surrogate key for the project, generated by the database and meaningful only inside this file."
        },
        "project_key": {
          "description": "Caller-supplied stable identifier for the project, unique within the database and declared explicitly at intake rather than inferred from the source hierarchy. This is the project's durable identity, so it survives a database rebuild in a way project_id does not."
        },
        "project_name": {
          "description": "Human-readable name for the project, used when reporting results to a person."
        },
        "description": {
          "description": "Free-text account of what the project covers. Never parsed by VAWLUME."
        },
        "created_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this row. An audit time, not a fact about when the research took place."
        },
        "archived_at_utc": {
          "description": "UTC timestamp recorded when the project was archived. Null while the project is still active."
        }
      }
    },
    "config_profiles": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "A named configuration profile: one interpretation contract, such as how a project's directory structure is read or how an extractor's output columns are understood. Rows elsewhere cite a profile version to record the assumptions under which they were produced.",
      "columns": {
        "profile_id": {
          "description": "VAWLUME surrogate key for the configuration profile, generated by the database."
        },
        "project_id": {
          "description": "Project that owns this profile. Null for a profile shipped with VAWLUME, which is available to every project in the database."
        },
        "profile_key": {
          "description": "Stable identifier for the profile, supplied by the profile document rather than generated. Together with the profile kind it forms the profile's durable identity, and it is unique within its project."
        },
        "profile_name": {
          "description": "Human-readable name for the profile, used when reporting which configuration produced a result."
        },
        "profile_kind": {
          "description": "Which interpretation contract this profile expresses, drawn from a declared vocabulary that separates source and project mapping, extractor output and settings, recording device, experimental setup, tracking and attribution input mapping, analysis settings, and policy. The kinds are deliberately distinct so that one profile cannot silently take on another's responsibility."
        },
        "is_builtin": {
          "description": "Whether VAWLUME shipped this profile (1) or a user authored it (0). Built-in profiles are seeded from the repository rather than registered from a user document."
        },
        "description": {
          "description": "Free-text account of what the profile interprets and under what assumptions. Never parsed by VAWLUME."
        },
        "created_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this row. An audit time, not a property of the profile document."
        }
      }
    },
    "config_profile_versions": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "One registered version of a configuration profile, carrying where its document was read from and a digest of its contents, so that any row citing this version can be traced back to the exact document that was in force when it was written.",
      "columns": {
        "profile_version_id": {
          "description": "VAWLUME surrogate key for this profile version, generated by the database. This is what other tables cite when they record which configuration governed them."
        },
        "profile_id": {
          "description": "Configuration profile that this is a version of."
        },
        "version_label": {
          "description": "Version as authored in the profile document itself. Unique within its profile and part of the version's durable identity, so republishing changed content under an existing label is refused rather than accepted."
        },
        "profile_schema_version": {
          "description": "Which revision of the profile document grammar this version was written against, as distinct from the version of the profile's own content. Null when the document declares none."
        },
        "content_format": {
          "description": "Serialization of the registered document, normally inferred from its file extension. VAWLUME's own configuration is JSON; the other permitted values exist so an externally authored document can be registered and cited without first being rewritten."
        },
        "content_uri": {
          "description": "Where the profile document was read from, stored repository-relative when the file lives under the repository root and absolute otherwise. The file is hashed, never copied, so this records a location rather than custody of the bytes."
        },
        "checksum_sha256": {
          "description": "SHA-256 of the profile document as registered. Re-registering this same version label over different bytes is reported as a conflict, because rows already cite this version by identifier and changing what it denotes would silently re-point their provenance."
        },
        "is_snapshot": {
          "description": "Caller-declared flag recording whether the registered content is a fixed snapshot rather than a reference expected to change underneath it. Defaults to true and is a statement about the document, not something VAWLUME verifies."
        },
        "created_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME registered this version. An audit time, not the date the profile was authored."
        },
        "notes": {
          "description": "Free-text remarks about this particular version. Never parsed by VAWLUME."
        }
      }
    },
    "project_profile_assignments": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "Records that a configuration profile version applies to a project in a named role, which is how a project declares the defaults its recordings can inherit.",
      "columns": {
        "project_id": {
          "description": "Project the profile version is assigned to."
        },
        "profile_version_id": {
          "description": "Exact profile version being assigned. A version, not a profile, so the assignment records which content was in force."
        },
        "assignment_role": {
          "description": "The role this profile version plays for the project. The same version can be assigned in more than one role, so the role is part of the assignment's identity rather than a label on it."
        },
        "is_default": {
          "description": "Whether this assignment is the project default for its role, and therefore a candidate for inheritance onto recordings that declare no assignment of their own."
        },
        "assigned_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this assignment. An audit time."
        }
      }
    },
    "source_files": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "A file VAWLUME discovered and registered as project source material, identified by its location within the project rather than by its absolute runtime path, so that relocating a project root reuses the same rows.",
      "columns": {
        "source_file_id": {
          "description": "VAWLUME surrogate key for the registered file, generated by the database."
        },
        "project_id": {
          "description": "Project this file was discovered within."
        },
        "file_role": {
          "description": "What this file is to the project, such as the audio a recording was made from or an artifact an extractor produced. It records how the file was claimed rather than what format it happens to be."
        },
        "path_or_uri": {
          "description": "Location the file was discovered at, as discovered, and unique within its project. Absolute runtime paths are used to prove a file exists for one execution and are deliberately not what intake treats as identity."
        },
        "relative_path": {
          "description": "Normalized portable path of the file within the project root. Together with the project this is what intake treats as the file's durable identity, which is why relocating a project root reuses the same rows rather than registering the files again."
        },
        "filename": {
          "description": "Base name of the file, separated out so that files sharing a name in different folders remain distinguishable while still being searchable by name."
        },
        "file_format": {
          "description": "Format of the file as recorded at discovery, normally derived from its extension. Not verified by reading the file."
        },
        "size_bytes": {
          "description": "Size of the file in bytes at discovery. Null when discovery did not record it."
        },
        "checksum_sha256": {
          "description": "SHA-256 of the file's contents at registration. Relocating the file is recognised as the same file; a change to its contents is a conflict rather than an overwrite. Null when no digest was taken."
        },
        "source_modified_at": {
          "description": "Modification time as reported by the source, in whatever form the source expressed it. Deliberately not recorded as UTC: VAWLUME does not know the zone it was expressed in and will not assert one."
        },
        "discovered_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME registered this file. An audit time, and distinct from the file's own modification time."
        }
      }
    },
    "ingestion_runs": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "One attempt to ingest a project's source material under a particular mapping profile version. Successful reruns create new rows rather than rewriting earlier ones, so the history of attempts survives even when the resulting graph is unchanged.",
      "columns": {
        "ingestion_run_id": {
          "description": "VAWLUME surrogate key for this ingestion attempt, generated by the database."
        },
        "project_id": {
          "description": "Project being ingested."
        },
        "mapping_profile_version_id": {
          "description": "Exact project-input mapping profile version that interpreted the sources for this attempt. This is what makes the resulting rows traceable to the rules that produced them."
        },
        "run_label": {
          "description": "Optional caller-supplied label distinguishing this attempt from others. Free text with no uniqueness requirement."
        },
        "vawlume_version": {
          "description": "Version of VAWLUME the caller asserted was in use. Supplied by the caller and never inferred from the working tree, so it records a claim rather than a verified fact."
        },
        "source_commit": {
          "description": "Source revision the caller asserted was in use. Like the version, a caller claim that VAWLUME records without checking."
        },
        "status": {
          "description": "State of the attempt, drawn from a declared vocabulary that separates one still in progress from one that completed, one that completed with warnings, and one that failed. Warnings are not errors and the distinction is kept."
        },
        "started_at_utc": {
          "description": "UTC timestamp recorded when the attempt began."
        },
        "completed_at_utc": {
          "description": "UTC timestamp recorded when the attempt finished. Null while the attempt is unfinished or if it never reached a terminal state."
        },
        "log_uri": {
          "description": "Location of an external log for this attempt, when the caller recorded one. VAWLUME stores the reference and does not read it."
        },
        "notes": {
          "description": "Free-text remarks about this attempt. Never parsed by VAWLUME."
        }
      }
    },
    "ingestion_files": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "Records how one source file fared in one ingestion attempt, so that a run's outcome can be read per file rather than only in aggregate.",
      "columns": {
        "ingestion_run_id": {
          "description": "Ingestion attempt this outcome belongs to."
        },
        "source_file_id": {
          "description": "Source file this outcome is about."
        },
        "parse_status": {
          "description": "How this file fared in this attempt, drawn from a declared vocabulary that separates a file not yet reached, a clean parse, a parse with warnings, a file deliberately skipped, and a failure. Skipped and failed are different outcomes and are kept apart."
        },
        "warning_count": {
          "description": "Number of warnings raised while parsing this file in this attempt. Zero means none were raised, not that none were possible."
        },
        "error_count": {
          "description": "Number of errors raised while parsing this file in this attempt."
        },
        "parser_message": {
          "description": "Human-readable summary of what the parser reported for this file. Diagnostic text, not a structured issue record."
        }
      }
    },
    "entity_types": {
      "kind": "table",
      "domain": "entities_experiment",
      "description": "One level of a project's own experimental hierarchy, such as its notion of a cohort, a subject, or a session, registered under the project's native name for it and optionally mapped to a canonical role VAWLUME understands.",
      "columns": {
        "entity_type_id": {
          "description": "VAWLUME surrogate key for this hierarchy level, generated by the database."
        },
        "project_id": {
          "description": "Project whose hierarchy this level belongs to. Levels are project-scoped because two projects may use the same word for different things."
        },
        "native_name": {
          "description": "The project's own name for this level, preserved as the source expressed it. Unique within the project and part of the level's durable identity."
        },
        "canonical_role": {
          "description": "The role this level is declared to fill, such as a study, a subject, or a session. An open vocabulary rather than a fixed one, so a project can declare a role VAWLUME does not model natively. This is the added canonical reading; the native name remains the source's own. Null when no canonical role was declared."
        },
        "hierarchy_order": {
          "description": "Where this level sits in the project's declared nesting, as the project ordered it. VAWLUME stores the ordering and neither computes nor enforces it. Null when the project declared no ordering."
        },
        "is_biological_unit": {
          "description": "Whether entities at this level are biological individuals rather than organizational groupings. Declared by the project, not inferred from the level's name."
        },
        "is_subject_like": {
          "description": "Whether entities at this level behave as subjects for analysis purposes. Kept separate from the biological flag because the two can disagree: an organizational unit may be analysed as a subject, and a biological individual may not be the unit of analysis."
        },
        "description": {
          "description": "Free-text account of what this level represents in the project. Never parsed by VAWLUME."
        }
      }
    },
    "experimental_entities": {
      "kind": "table",
      "domain": "entities_experiment",
      "description": "One thing in a project's experimental hierarchy — a particular cohort, subject, or session — identified by the project's own name for it at its own level.",
      "columns": {
        "entity_id": {
          "description": "VAWLUME surrogate key for this entity, generated by the database."
        },
        "project_id": {
          "description": "Project this entity belongs to."
        },
        "entity_type_id": {
          "description": "Hierarchy level this entity sits at, which is what makes its native identifier interpretable."
        },
        "native_id": {
          "description": "The project's own identifier for this entity, preserved as the source expressed it. Unique within its level, and together with the level it forms the entity's durable identity."
        },
        "display_label": {
          "description": "Human-readable label for the entity when the native identifier is not itself readable. Null when none was supplied."
        },
        "ingestion_run_id": {
          "description": "Ingestion attempt that first created this row. Later compatible attempts record their own audit rows rather than rewriting this provenance, so this names the attempt the entity entered the database on."
        },
        "created_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this row. An audit time, not a fact about the experiment."
        }
      }
    },
    "entity_relationships": {
      "kind": "table",
      "domain": "entities_experiment",
      "description": "A declared relationship between two entities in a project's hierarchy, most often containment, together with the source evidence and mapping rule that produced it. An entity may not be related to itself.",
      "columns": {
        "entity_relationship_id": {
          "description": "VAWLUME surrogate key for this relationship, generated by the database."
        },
        "project_id": {
          "description": "Project both endpoints belong to."
        },
        "parent_entity_id": {
          "description": "The containing or superior entity of the pair. Which endpoint is the parent is part of the relationship's meaning, not an arbitrary ordering."
        },
        "child_entity_id": {
          "description": "The contained or subordinate entity of the pair, and never the same entity as the parent."
        },
        "relationship_type": {
          "description": "What the relationship asserts between the two endpoints, defaulting to containment. Free text so a project can declare a relationship VAWLUME does not model natively."
        },
        "role_label": {
          "description": "The role the child plays in this relationship when the type alone is not specific enough. Part of the relationship's durable identity, so the same pair can be related more than once in different roles."
        },
        "valid_from_utc": {
          "description": "UTC time from which this relationship is asserted to hold. Null when the relationship is not time-bounded at its start."
        },
        "valid_to_utc": {
          "description": "UTC time until which this relationship is asserted to hold. Null when the relationship is open-ended."
        },
        "ingestion_run_id": {
          "description": "Ingestion attempt that first created this row. Null when the relationship did not arise from an ingestion attempt."
        },
        "source_file_id": {
          "description": "Source file this relationship was read from, when it came from one. Null when the relationship has no single originating file."
        },
        "source_locator": {
          "description": "Where within the source this relationship was read from, recorded so the assertion can be traced back to the text that produced it."
        },
        "mapping_rule_key": {
          "description": "Which rule in the mapping profile claimed this relationship, so the interpretation is traceable to the rule that made it rather than only to the profile as a whole."
        }
      }
    },
    "entity_attributes": {
      "kind": "table",
      "domain": "entities_experiment",
      "description": "One attribute of one entity, stored in a typed column chosen by its declared value type, together with the source location and mapping rule it came from. Values reach this table because a mapping rule claimed them as attributes, not because they appeared in a source file.",
      "columns": {
        "entity_attribute_id": {
          "description": "VAWLUME surrogate key for this attribute row, generated by the database."
        },
        "entity_id": {
          "description": "Entity this attribute describes."
        },
        "attribute_name": {
          "description": "The attribute's name as the source expressed it, preserved rather than normalized."
        },
        "value_type": {
          "description": "Which of the typed value columns carries this attribute's value, or the declared absence of a value. A constraint enforces that exactly one typed column is populated, and that none is when the value is absent, so a missing value can never be confused with an empty one."
        },
        "value_text": {
          "description": "The attribute's value when it is textual, and null otherwise."
        },
        "value_real": {
          "description": "The attribute's value when it is a real number, and null otherwise."
        },
        "value_integer": {
          "description": "The attribute's value when it is an integer, and null otherwise."
        },
        "value_boolean": {
          "description": "The attribute's value when it is a truth value, stored as 0 or 1, and null otherwise."
        },
        "value_json": {
          "description": "The attribute's value when it is a structured payload, stored as JSON text, and null otherwise."
        },
        "unit": {
          "description": "Unit the source expressed the value in, as the source expressed it. Free text that VAWLUME records and never interprets or converts."
        },
        "ingestion_run_id": {
          "description": "Ingestion attempt that first created this row. Null when the attribute did not arise from an ingestion attempt."
        },
        "source_file_id": {
          "description": "Source file this attribute was read from, when it came from one."
        },
        "source_locator": {
          "description": "Where within the source this value was read from, recorded so it can be traced back to the text it came from."
        },
        "mapping_rule_key": {
          "description": "Which rule in the mapping profile claimed this value, so the interpretation is traceable to the rule that made it."
        }
      }
    },
    "recordings": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "One audio recording, anchored to exactly one registered source file. That anchor is the boundary extractor imports resolve against, so a recording is identified by the file it was made from rather than by any name an extractor gives it.",
      "columns": {
        "recording_id": {
          "description": "VAWLUME surrogate key for the recording, generated by the database."
        },
        "project_id": {
          "description": "Project this recording belongs to."
        },
        "source_file_id": {
          "description": "The registered audio file this recording is made from, unique across recordings. This one-to-one anchor is the recording's durable identity and the point extractor importers attach their runs to."
        },
        "native_recording_id": {
          "description": "The source's own identifier for this recording, preserved as expressed. Null when the source names recordings only by filename."
        },
        "checksum_sha256": {
          "description": "SHA-256 of the audio as registered. Null in the ordinary case, because intake does not open audio files; it is populated only when some adapter supplied the digest."
        },
        "sample_rate_hz": {
          "description": "Sampling rate in hertz, when it is known. Null in the ordinary case: intake reads no audio metadata, so this is populated only when an adapter supplied it rather than being measured from the file."
        },
        "bit_depth": {
          "description": "Bits per sample, when it is known. Like the sampling rate, supplied rather than measured, and null when nothing supplied it."
        },
        "channel_count": {
          "description": "Number of channels the recording is declared to have, when it is known. Supplied rather than measured. A declared channel index above this count is refused; a recording declaring no count is not checked at all."
        },
        "duration_s": {
          "description": "Duration in seconds, when it is known. Supplied rather than measured, and null in the ordinary case."
        },
        "recorded_start_utc": {
          "description": "UTC time the recording is asserted to have started. An assertion about the world, unlike the audit timestamp on this row, and null when nothing asserted it."
        },
        "created_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this row. An audit time, and not to be read as when the recording was made."
        },
        "notes": {
          "description": "Free-text remarks about the recording. Never parsed by VAWLUME."
        }
      }
    },
    "recording_channels": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "One channel of one recording. A channel row is a caller assertion that the recording has that channel; nothing inspects the audio to confirm it, and the microphone that produced it is placed in space by a separate row.",
      "columns": {
        "recording_channel_id": {
          "description": "VAWLUME surrogate key for this channel, generated by the database."
        },
        "recording_id": {
          "description": "Recording this channel belongs to. A channel belongs to exactly one recording, which is what makes anything attached to it session-specific."
        },
        "channel_index": {
          "description": "Position of this channel within the recording, counting from one, and unique within the recording. Refused when it exceeds a declared channel count."
        },
        "channel_label": {
          "description": "Human-readable label for the channel as the source named it. Null when the source named it only by index."
        },
        "channel_role": {
          "description": "What this channel is for, when the caller declared a role for it. Free text, and null when no role was declared."
        }
      }
    },
    "recording_entity_links": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "Records that an entity took part in a recording in a named role, optionally over a bounded interval within it. This is how the experimental hierarchy reaches individual recordings.",
      "columns": {
        "recording_entity_link_id": {
          "description": "VAWLUME surrogate key for this participation record, generated by the database."
        },
        "recording_id": {
          "description": "Recording the entity took part in."
        },
        "entity_id": {
          "description": "Entity that took part."
        },
        "link_type": {
          "description": "The nature of the entity's involvement, defaulting to participation. Free text so a project can declare an involvement VAWLUME does not model natively, and part of the link's durable identity."
        },
        "role_label": {
          "description": "The role the entity played, when the link type alone is not specific enough. Part of the link's durable identity, so one entity can be linked to one recording more than once in different roles."
        },
        "start_time_s": {
          "description": "Seconds from the start of the recording at which the entity's involvement begins. Null when the involvement is not time-bounded, which is the ordinary case for a participant present throughout."
        },
        "end_time_s": {
          "description": "Seconds from the start of the recording at which the entity's involvement ends, never before the start. Null when the involvement is open-ended."
        },
        "ingestion_run_id": {
          "description": "Ingestion attempt that first created this row. Null when the link did not arise from an ingestion attempt."
        }
      }
    },
    "recording_attributes": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "One attribute of one recording, stored in a typed column chosen by its declared value type, together with the source location and mapping rule it came from. The same typed-value shape as entity attributes, scoped to a recording rather than to a participant in the hierarchy.",
      "columns": {
        "recording_attribute_id": {
          "description": "VAWLUME surrogate key for this attribute row, generated by the database."
        },
        "recording_id": {
          "description": "Recording this attribute describes."
        },
        "attribute_name": {
          "description": "The attribute's name as the source expressed it, preserved rather than normalized."
        },
        "value_type": {
          "description": "Which of the typed value columns carries this attribute's value, or the declared absence of a value. A constraint enforces that exactly one typed column is populated, and that none is when the value is absent, so a missing value can never be confused with an empty one."
        },
        "value_text": {
          "description": "The attribute's value when it is textual, and null otherwise."
        },
        "value_real": {
          "description": "The attribute's value when it is a real number, and null otherwise."
        },
        "value_integer": {
          "description": "The attribute's value when it is an integer, and null otherwise."
        },
        "value_boolean": {
          "description": "The attribute's value when it is a truth value, stored as 0 or 1, and null otherwise."
        },
        "value_json": {
          "description": "The attribute's value when it is a structured payload, stored as JSON text, and null otherwise."
        },
        "unit": {
          "description": "Unit the source expressed the value in, as the source expressed it. Free text that VAWLUME records and never interprets or converts."
        },
        "ingestion_run_id": {
          "description": "Ingestion attempt that first created this row. Null when the attribute did not arise from an ingestion attempt."
        },
        "source_file_id": {
          "description": "Source file this attribute was read from, when it came from one."
        },
        "source_locator": {
          "description": "Where within the source this value was read from, recorded so it can be traced back to the text it came from."
        },
        "mapping_rule_key": {
          "description": "Which rule in the mapping profile claimed this value, so the interpretation is traceable to the rule that made it."
        }
      }
    },
    "entity_profile_assignments": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "Records that a configuration profile version applies to one entity in a named role, so that a subject or session can carry configuration its project default does not supply.",
      "columns": {
        "entity_id": {
          "description": "Entity the profile version is assigned to."
        },
        "profile_version_id": {
          "description": "Exact profile version being assigned, so the assignment records which content was in force rather than only which profile."
        },
        "assignment_role": {
          "description": "The role this profile version plays for the entity. Part of the assignment's identity, so one version can be assigned in more than one role."
        },
        "assigned_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this assignment. An audit time."
        }
      }
    },
    "recording_profile_assignments": {
      "kind": "table",
      "domain": "identity_config_provenance",
      "description": "Records that a configuration profile version applies to one recording in a named role, and how the recording came to have it. This is where an inherited default becomes an explicit, readable fact about a particular recording.",
      "columns": {
        "recording_id": {
          "description": "Recording the profile version is assigned to."
        },
        "profile_version_id": {
          "description": "Exact profile version being assigned, so the assignment records which content was in force."
        },
        "assignment_role": {
          "description": "The role this profile version plays for the recording. Part of the assignment's identity, so one version can be assigned in more than one role."
        },
        "inheritance_source": {
          "description": "How this recording came to have the assignment: declared directly on it, inherited from a project default, or inherited through an entity it is linked to. Recorded so that an inherited assignment is never mistaken for one the user declared."
        },
        "assigned_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME created this assignment. An audit time."
        }
      }
    },
    "recording_epochs": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "A named interval within a recording, such as a phase of a protocol, optionally nested inside another interval of the same recording. Epochs are declared or imported rather than detected by VAWLUME.",
      "columns": {
        "epoch_id": {
          "description": "VAWLUME surrogate key for this interval, generated by the database."
        },
        "recording_id": {
          "description": "Recording this interval falls within."
        },
        "parent_epoch_id": {
          "description": "Enclosing interval, when this one is nested inside another of the same recording. Null for a top-level interval."
        },
        "epoch_name": {
          "description": "Name of the interval as declared. Together with the recording and the interval bounds it forms the epoch's durable identity."
        },
        "epoch_type": {
          "description": "What kind of interval this is, when the caller declared a kind. Free text, and null when none was declared."
        },
        "start_time_s": {
          "description": "Seconds from the start of the recording at which this interval begins."
        },
        "end_time_s": {
          "description": "Seconds from the start of the recording at which this interval ends, never before the start."
        },
        "source": {
          "description": "Where the interval came from, defaulting to manual declaration or import. VAWLUME does not detect epochs itself, so this records who said so."
        },
        "source_file_id": {
          "description": "Source file this interval was read from, when it was imported from one. Null when it was declared directly."
        },
        "notes": {
          "description": "Free-text remarks about the interval. Never parsed by VAWLUME."
        }
      }
    },
    "coordinate_systems": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "A declared spatial frame that positions within a project are expressed in. Two spatial facts are comparable if and only if they cite this same frame by identifier; VAWLUME implements no transformation between frames, so structural similarity between two frames never makes them interchangeable.",
      "columns": {
        "coordinate_system_id": {
          "description": "VAWLUME surrogate key for this frame, generated by the database. Comparability between two spatial facts is decided by comparing this identifier and nothing else."
        },
        "project_id": {
          "description": "Project this frame is declared for. Frames are project-scoped shared reference data, cited by anything positioned within the project."
        },
        "coordinate_system_key": {
          "description": "Stable identifier for the frame as the project names it, unique within the project."
        },
        "coordinate_system_name": {
          "description": "Human-readable name for the frame, used when reporting which space a position was expressed in."
        },
        "dimensionality": {
          "description": "Whether the frame is two- or three-dimensional. Two is the working case; three is representable and never required, and a height may only be recorded under a three-dimensional frame."
        },
        "unit": {
          "description": "Unit positions in this frame are expressed in, held on the frame so that two comparable facts cannot disagree about it. Free text that VAWLUME never interprets or converts: a pixel frame is legal and supports no real-distance computation, and pixels are never treated as a metric unit."
        },
        "origin_description": {
          "description": "Human-readable account of where the frame's origin sits. Provenance for a reader, never applied by any computation."
        },
        "orientation_description": {
          "description": "Human-readable account of how the frame's axes are oriented. Provenance for a reader, never applied by any computation."
        },
        "notes": {
          "description": "Free-text remarks about the frame. Never parsed by VAWLUME."
        }
      }
    },
    "channel_placements": {
      "kind": "table",
      "domain": "recordings_geometry",
      "description": "Where one recording channel's microphone was, expressed in one declared frame. The placement attaches to a channel rather than to a device or a setup profile, so it is specific to the session that channel belongs to and cannot claim a position outliving it.",
      "columns": {
        "channel_placement_id": {
          "description": "VAWLUME surrogate key for this placement, generated by the database."
        },
        "recording_channel_id": {
          "description": "Channel whose microphone this places, at most one placement per channel. Because a channel belongs to exactly one recording, the placement is session-specific by construction."
        },
        "coordinate_system_id": {
          "description": "Frame the coordinates below are expressed in. A placement and the frame it cites must belong to the same project, and a cited frame cannot be deleted while a placement still names it."
        },
        "position_x": {
          "description": "Position along the frame's first axis, in the frame's declared unit."
        },
        "position_y": {
          "description": "Position along the frame's second axis, in the frame's declared unit."
        },
        "position_z": {
          "description": "Height in the frame's declared unit, permitted only under a three-dimensional frame and optional even then. Null means the height is unknown rather than zero."
        },
        "placement_role": {
          "description": "What this microphone was for within the arrangement, when the caller declared a role. Free text, and null when none was declared."
        },
        "orientation_description": {
          "description": "Human-readable account of which way the microphone faced. Provenance for a reader, never applied by any computation."
        },
        "source_profile_version_id": {
          "description": "Device or setup profile version this placement was authored from, when one was cited. Recorded as provenance only: a reusable profile describes many sessions and cannot itself answer where a microphone was during this one."
        },
        "notes": {
          "description": "Free-text remarks about the placement. Never parsed by VAWLUME."
        }
      }
    },
    "extractors": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "An external vocalization-detection tool whose output VAWLUME can ingest, registered once and shared across every project in the database. VAWLUME does not run these tools; it interprets what they produced.",
      "columns": {
        "extractor_id": {
          "description": "VAWLUME surrogate key for the tool, generated by the database."
        },
        "extractor_key": {
          "description": "Stable identifier for the tool, unique across the database. Composed pair and set keys elsewhere are built by joining these with delimiter sequences, so a key containing one of those sequences is refused by the layer that composes them rather than parsing ambiguously."
        },
        "extractor_name": {
          "description": "Human-readable name of the tool as its authors write it. Subject to the same delimiter restriction as the key, because pair labels are composed from names."
        },
        "description": {
          "description": "Free-text account of what the tool does and what it emits. Never parsed by VAWLUME."
        },
        "source_repository": {
          "description": "Where the tool's own source lives, recorded as a reference for a reader. VAWLUME never fetches or inspects it."
        }
      }
    },
    "extractor_versions": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "One version of an external extractor. Runs cite a version rather than the tool, because two versions of one detector may disagree about the same audio and pooling their output would erase that difference.",
      "columns": {
        "extractor_version_id": {
          "description": "VAWLUME surrogate key for this version, generated by the database. This is what an extraction run and a declared feature both cite."
        },
        "extractor_id": {
          "description": "Tool this is a version of."
        },
        "version_label": {
          "description": "Version as reported for the tool. Required when importing any of the three supported extractors and supplied by the caller, because the exports VAWLUME reads carry no trustworthy version of their own."
        },
        "source_commit_or_tag": {
          "description": "Source revision the version corresponds to, when the caller knew it. Part of the version's uniqueness, so one label built from two revisions remains two rows."
        },
        "build_identifier": {
          "description": "Build or distribution identifier for this version, when one exists. Null when the tool publishes none."
        },
        "implementation_language": {
          "description": "Language the tool is implemented in, recorded as context for a reader rather than used by anything."
        },
        "notes": {
          "description": "Free-text remarks about this version. Never parsed by VAWLUME."
        }
      }
    },
    "artifacts": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "A file an extractor produced or consumed, registered as provenance for a run: an export VAWLUME parsed, a settings file, a detector model, or a native container recorded without being read. Registration is a citation, not custody of the contents.",
      "columns": {
        "artifact_id": {
          "description": "VAWLUME surrogate key for the artifact, generated by the database."
        },
        "project_id": {
          "description": "Project this artifact was registered under. Its location is unique within the project rather than globally."
        },
        "source_file_id": {
          "description": "Registered source file this artifact is, when the same file was also discovered as project source material. Null when the artifact was registered only as extractor provenance."
        },
        "parent_artifact_id": {
          "description": "Artifact this one was derived from, such as the native container an export was written out of. Null for an artifact with no recorded antecedent."
        },
        "artifact_type": {
          "description": "What role this file plays for VAWLUME, such as the export that was parsed, the settings evidence for a run, or a detector model. This is the reading VAWLUME acts on."
        },
        "native_artifact_type": {
          "description": "What the extractor itself calls this kind of file, preserved alongside VAWLUME's reading of it rather than replaced by it."
        },
        "path_or_uri": {
          "description": "Location the artifact was registered at, unique within its project."
        },
        "file_format": {
          "description": "Format of the file as recorded at registration, normally derived from its extension. Not established by reading the file."
        },
        "checksum_sha256": {
          "description": "SHA-256 of the file's contents at registration. Relocating the file is recognised as the same file; a change to its contents is a conflict rather than an overwrite. Null when no digest was taken."
        },
        "is_native": {
          "description": "Whether this is the extractor's own native container (1) or a derived export (0). A native container is registered as provenance and is generally not parsed."
        },
        "created_at_source": {
          "description": "Creation time as the source reported it, in whatever form the source expressed it. Deliberately not recorded as UTC: VAWLUME does not know the zone it was expressed in and will not assert one."
        },
        "imported_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME registered this artifact. An audit time, and distinct from when the extractor created the file."
        },
        "metadata_json": {
          "description": "Extractor-native provenance preserved as JSON text, such as the workspace and dataset names a tool organized its output under. Recorded so the information survives, and deliberately never turned into experimental hierarchy rows."
        }
      }
    },
    "extraction_runs": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "One execution of one extractor version over declared input recordings. This is the unit that must never be pooled: each run may detect a different number of calls, and detections from separate runs are separate observations of the audio rather than one biological event set.",
      "columns": {
        "extraction_run_id": {
          "description": "VAWLUME surrogate key for the run, generated by the database. Detections, measurements and every downstream analysis cite this to keep one run's output separable from another's."
        },
        "project_id": {
          "description": "Project the run belongs to. The run key is unique within the project rather than globally."
        },
        "extractor_version_id": {
          "description": "Exact extractor version that produced this run's output. A version rather than a tool, so two versions' output can never be conflated."
        },
        "run_key": {
          "description": "Caller-supplied stable identifier for the run, unique within its project and required at import. This is the run's durable identity. Composed set keys elsewhere are built from it, so a key containing one of the sequences those are joined with is refused by the layer that composes them, as it is for extractor keys and names."
        },
        "run_label": {
          "description": "Optional human-readable label for the run. Free text with no uniqueness requirement."
        },
        "output_mapping_profile_version_id": {
          "description": "Profile version that interpreted this run's export columns. Held separately from the settings profile and never conflated with it: one says how the output was read, the other says how the tool was configured."
        },
        "settings_profile_version_id": {
          "description": "Profile version recording how the extractor was configured for this run. Null means the settings were not recoverable, which is recorded as an absence rather than filled with a default the tool may not have used."
        },
        "started_at_utc": {
          "description": "UTC time the extractor run began, when the caller knew it. Null when the export carries no such evidence."
        },
        "completed_at_utc": {
          "description": "UTC time the extractor run finished, when the caller knew it. Null when the export carries no such evidence."
        },
        "imported_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME imported this run. An audit time about the import, not about the extraction."
        },
        "status": {
          "description": "State of the run, drawn from a declared vocabulary that separates one merely planned, one still running, one the extractor completed, one VAWLUME has imported, and one that failed. Import is the ordinary state for a run whose output was read from an existing export."
        },
        "notes": {
          "description": "Free-text remarks about the run, including provenance the schema has no column for, such as the source row order of an export. Never parsed by VAWLUME."
        }
      }
    },
    "extraction_run_inputs": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Records that a recording, optionally one channel of it, was an input to an extraction run. A detection may only name a recording registered here; a trigger refuses one that is not, so a run cannot acquire detections from audio it never declared reading.",
      "columns": {
        "extraction_run_id": {
          "description": "Run that consumed this input."
        },
        "recording_id": {
          "description": "Recording the run consumed."
        },
        "recording_channel_id": {
          "description": "Particular channel consumed, when the run read one channel rather than the recording as a whole. Null for a whole-recording input."
        },
        "input_role": {
          "description": "How the run used this input, defaulting to source audio. Part of the input's identity, so one recording can be supplied to a run in more than one role."
        }
      }
    },
    "extraction_run_profiles": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Records that a configuration profile version applies to an extraction run in a named role, and how the run came to have it. This is where a profile inherited from a recording or a project default becomes an explicit fact about a particular run.",
      "columns": {
        "extraction_run_id": {
          "description": "Run the profile version is assigned to."
        },
        "profile_version_id": {
          "description": "Exact profile version being assigned, so the assignment records which content was in force."
        },
        "assignment_role": {
          "description": "The role this profile version plays for the run. Part of the assignment's identity, so one version can be assigned in more than one role."
        },
        "inheritance_source": {
          "description": "How this run came to have the assignment: declared directly on it, or inherited from the recording, a project default, or an entity. Recorded so that an inherited assignment is never mistaken for one the user declared."
        }
      }
    },
    "extraction_run_artifacts": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Records that a registered artifact belongs to an extraction run in a named role, which is how a run cites the export it was read from alongside the settings and model evidence supporting it.",
      "columns": {
        "extraction_run_id": {
          "description": "Run the artifact belongs to."
        },
        "artifact_id": {
          "description": "Artifact being cited."
        },
        "artifact_role": {
          "description": "What this artifact is to the run, such as the parsed export or supporting evidence. Part of the citation's identity, so one artifact can serve a run in more than one role."
        }
      }
    },
    "extractor_objects": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "One node of the container hierarchy an extractor emitted in its own terms, such as its notion of an audio file, a processing pass, or a syllable set, preserved alongside VAWLUME's reading of what that node corresponds to. Rows exist only where an extractor's profile declares an emitted hierarchy; an export that declares none produces no rows here.",
      "columns": {
        "extractor_object_id": {
          "description": "VAWLUME surrogate key for this node, generated by the database."
        },
        "extraction_run_id": {
          "description": "Run whose output this node came from. The hierarchy is per run, because it is the extractor's own organization of that run's output."
        },
        "parent_object_id": {
          "description": "Enclosing node, when this one is nested inside another from the same run. Null for a top-level node, which is what makes the nesting readable without a separate depth column."
        },
        "source_artifact_id": {
          "description": "Artifact this node was read from. Null when the node was inferred from the run rather than from a particular file."
        },
        "native_level": {
          "description": "The extractor's own word for this level of its hierarchy, preserved as the tool expressed it rather than translated."
        },
        "canonical_level": {
          "description": "VAWLUME's reading of what the native level corresponds to, such as a recording, an extraction run, or an event. The added canonical interpretation; the native level remains the tool's own."
        },
        "canonical_subtype": {
          "description": "Finer canonical reading within the level, where the level alone does not say enough about what the node holds. Null when no subtype was assigned."
        },
        "equivalence_class": {
          "description": "Label grouping nodes that different extractors express differently but that mean the same thing, so that two tools' hierarchies can be compared without either being rewritten into the other's vocabulary."
        },
        "native_id": {
          "description": "The extractor's own identifier for this node, preserved as expressed. Unique within its run, level and parent, which is scoping rather than a claim of durable cross-artifact identity."
        },
        "native_label": {
          "description": "The extractor's own human-readable name for this node, when it provided one."
        },
        "mapping_strength": {
          "description": "How firmly the native level was read as the canonical one, recorded because that reading is an interpretation and its confidence varies between tools rather than being uniform."
        },
        "lineage_note": {
          "description": "Free-text account of where this node came from and how it was interpreted. Never parsed by VAWLUME."
        }
      }
    },
    "extractor_object_recordings": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Records which recordings one extractor-native node covers. A separate table because an extractor container may span several recordings, so the relationship cannot be a single column on the node.",
      "columns": {
        "extractor_object_id": {
          "description": "Extractor-native node being mapped to a recording."
        },
        "recording_id": {
          "description": "Recording the node covers."
        },
        "membership_role": {
          "description": "How the recording relates to the node, defaulting to plain membership. Part of the mapping's identity, so one recording can relate to a node in more than one way."
        }
      }
    },
    "canonical_features": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "VAWLUME's own vocabulary of measurable quantities, defined once so a native measurement can be given an interpretable reading in shared terms. Sharing a canonical feature is not what makes two extractors' measurements comparable: that judgement is registered separately, pair by pair, and two tools computing a quantity by different methods are deliberately given different canonical names rather than merged under one.",
      "columns": {
        "canonical_feature_id": {
          "description": "VAWLUME surrogate key for the canonical feature, generated by the database."
        },
        "canonical_name": {
          "description": "VAWLUME's name for the quantity, unique across the database. Names a definition rather than asserting comparability: two extractors measuring nominally the same thing by different methods carry different canonical names on purpose, because merging them would assert that the two statistics are one."
        },
        "feature_domain": {
          "description": "Broad area the quantity belongs to, such as timing or spectral content, used to group the vocabulary for a reader."
        },
        "value_type": {
          "description": "Kind of value this feature takes, which determines which typed column a measurement of it populates."
        },
        "canonical_unit": {
          "description": "Unit VAWLUME expresses this feature in, which is what a native unit is converted to when a mapping declares a transform. Null for a feature carrying no unit."
        },
        "definition": {
          "description": "What the quantity means, written so that two extractors' measurements can be judged against one definition rather than against each other."
        },
        "is_vawlume_derived": {
          "description": "Whether VAWLUME defines this feature itself (1) rather than adopting it from what extractors already emit (0). A derived feature is VAWLUME's own product and should not be read as an extractor observation."
        }
      }
    },
    "extractor_features": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "One quantity that a particular extractor version declares it emits, described in the extractor's own terms. Registration is what makes a measurement interpretable; a column an extractor writes but no version declares here has no registered meaning.",
      "columns": {
        "extractor_feature_id": {
          "description": "VAWLUME surrogate key for this declared feature, generated by the database. Every measurement cites it, which is how a stored number keeps the identity of what produced it."
        },
        "extractor_version_id": {
          "description": "Extractor version that declares this feature. Declared per version because what a tool emits, and what it means by it, can change between versions."
        },
        "native_name": {
          "description": "The extractor's own name for the quantity, preserved exactly as the tool writes it."
        },
        "native_unit": {
          "description": "Unit the extractor expresses this quantity in, as the extractor expresses it. Preserved rather than converted; conversion, where it happens at all, is recorded on the measurement."
        },
        "value_type": {
          "description": "Kind of value this feature takes, which determines which typed column a native measurement of it populates."
        },
        "native_definition": {
          "description": "What the extractor says the quantity means, in the extractor's own terms. The basis on which any claim of comparability with another tool's feature has to be judged."
        },
        "source_artifact_type": {
          "description": "Which kind of artifact this feature appears in, since one extractor may emit different quantities in different outputs."
        },
        "derivation_stage": {
          "description": "How far through the extractor's own processing this quantity is produced, which matters because a raw and a post-processed version of nominally the same quantity are not the same measurement."
        },
        "measurement_method": {
          "description": "How the extractor arrives at the value, recorded because two tools reporting the same nominal quantity by different methods may not be comparable."
        },
        "operational_variant": {
          "description": "Which variant of a quantity this is, where one extractor emits more than one operational definition of nominally the same thing. A measurement join must select the intended variant rather than assume a canonical feature has exactly one native counterpart."
        },
        "equivalence_class": {
          "description": "Label grouping features that different extractors express differently but that refer to the same underlying quantity, used as a starting point for comparability rather than as a claim of it."
        },
        "source_reference": {
          "description": "Where in the extractor's own documentation this feature is described, recorded so a reader can check the native definition against its source."
        },
        "notes": {
          "description": "Free-text remarks about this declared feature. Never parsed by VAWLUME."
        }
      }
    },
    "feature_mappings": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Declares how one extractor's feature relates to one of VAWLUME's canonical features, under a particular mapping profile version. This is a recorded judgement about correspondence, including the judgement that two things do not correspond.",
      "columns": {
        "feature_mapping_id": {
          "description": "VAWLUME surrogate key for this mapping, generated by the database."
        },
        "extractor_feature_id": {
          "description": "Extractor-declared feature being mapped."
        },
        "canonical_feature_id": {
          "description": "Canonical feature it is mapped to."
        },
        "mapping_profile_version_id": {
          "description": "Profile version that declared this mapping, so a correspondence judgement is attributable to the document that made it. Part of the mapping's uniqueness, so two profile versions may declare different mappings for one pair. Null for a mapping seeded with the repository rather than authored."
        },
        "mapping_type": {
          "description": "How strong the correspondence is, drawn from a declared vocabulary running from convertible by a stated transform, through conceptually equivalent, comparable, and merely related, to explicitly not comparable. Recording that two features are not comparable is a result, not an omission."
        },
        "transform_key": {
          "description": "Named transform converting the native value to the canonical one, from a fixed whitelist rather than an arbitrary function name. Null when no conversion applies."
        },
        "preserve_raw": {
          "description": "Whether the native value is retained alongside any converted one. Defaults to true, because canonicalization is meant to add an interpretable reading rather than replace what the extractor said."
        },
        "notes": {
          "description": "Free-text justification or caveat for this mapping. Never parsed by VAWLUME."
        }
      }
    },
    "feature_relationships": {
      "kind": "table",
      "domain": "extractors_features",
      "description": "Declares how one extractor's feature relates to another extractor's feature directly, rather than through the canonical vocabulary. This registry is what decides whether a cross-extractor comparison of a quantity is admissible at all.",
      "columns": {
        "feature_relationship_id": {
          "description": "VAWLUME surrogate key for this pairing, generated by the database."
        },
        "feature_a_id": {
          "description": "One endpoint of the pairing. The a and b endpoints are ordered by identifier rather than by role, which is a constraint that keeps one conceptual pairing from being stored twice in opposite directions; neither side is privileged and any asymmetry of role is recorded separately."
        },
        "feature_b_id": {
          "description": "The other endpoint of the pairing, always the higher identifier of the two. As with the a endpoint, the position carries ordering rather than meaning."
        },
        "relationship_type": {
          "description": "How strong the correspondence is, drawn from the same declared vocabulary the canonical mappings use, running from convertible by a stated transform to explicitly not comparable."
        },
        "comparison_method": {
          "description": "How the two features may legitimately be compared, when the relationship type alone does not say. Recorded because two tools measuring nominally the same quantity by different methods may need a stated basis for comparison."
        },
        "unit_normalization": {
          "description": "What must be done to the two sides' units before their values can be compared. Null when the sides already agree."
        },
        "consilience_eligible": {
          "description": "Whether the registry considers this pairing defensible enough to compare. Necessary but not sufficient: a comparison also requires the two sides' canonical units to agree. An ineligible pairing is still reported as one that exists, and is excluded from every comparison and summary, so exclusion is visible rather than silent."
        },
        "default_role": {
          "description": "Which side is treated as the reference when a comparison needs one. This is where an asymmetry of role is recorded, since the endpoint ordering itself carries none."
        },
        "justification": {
          "description": "Why this correspondence is claimed, written so that the claim can be assessed rather than taken. Never parsed by VAWLUME."
        },
        "source_reference": {
          "description": "Where the evidence for this correspondence comes from, recorded so a reader can check it against its source."
        }
      }
    },
    "detections": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "One event an extractor reported in one recording during one run, with its timing as the extractor gave it. A detection is an observation by a particular tool, not an established vocalization: two runs over the same audio may report different numbers of them and neither is the truth the other failed to find.",
      "columns": {
        "detection_id": {
          "description": "VAWLUME surrogate key for the detection, generated by the database."
        },
        "extraction_run_id": {
          "description": "Run that reported this detection. Detections are only ever compared across runs through the matching layer, never pooled by being read together."
        },
        "recording_id": {
          "description": "Recording the event was reported in. Must already be registered as an input to the run, which a trigger enforces."
        },
        "source_artifact_id": {
          "description": "Artifact this detection was read from. Part of the detection's uniqueness within its run, so one run reading two exports keeps their events distinct."
        },
        "extractor_object_id": {
          "description": "Node of the extractor's own hierarchy this detection sits under, when the extractor emitted one. Null for an export that declares no container hierarchy."
        },
        "native_event_id": {
          "description": "The extractor's own identifier for the event, preserved as expressed. Often an ordinal within one export rather than a durable identifier: the same values may legally recur in another run or another tool's output, so it identifies the event only within its run, recording and artifact."
        },
        "event_subtype": {
          "description": "What kind of event this row records, defaulting to a vocalization detection. Present so the table can hold event kinds other than calls without a parallel table."
        },
        "start_time_s": {
          "description": "Onset in seconds from the start of the recording, as the extractor reported it. Never rewritten by alignment; a time expressed on another clock is derived on demand from stored coefficients instead."
        },
        "end_time_s": {
          "description": "Offset in seconds from the start of the recording, never before the onset, as the extractor reported it."
        },
        "timing_basis": {
          "description": "Which of the extractor's own quantities the stored onset and offset were taken from, recorded because an export may carry several timing columns and the one chosen is a mapping decision rather than an obvious fact."
        },
        "detection_score": {
          "description": "The extractor's own confidence or model score for this event, on the extractor's own scale and uncalibrated. Null when the extractor emits no score at all, which is the case for two of the three supported tools; absence here is a property of the producer rather than a failed import."
        },
        "imported_at_utc": {
          "description": "UTC timestamp recorded when VAWLUME imported this detection. An audit time about the import, not about the event."
        },
        "notes": {
          "description": "Free-text remarks about the detection, including provenance with no column of its own such as the source row position it was read from. Never parsed by VAWLUME."
        }
      }
    },
    "event_measurements": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "One measured quantity for one detection, holding the extractor's value and VAWLUME's canonicalized reading of it side by side. Both are kept because canonicalization adds an interpretable representation and is never allowed to erase what the extractor actually said.",
      "columns": {
        "event_measurement_id": {
          "description": "VAWLUME surrogate key for this measurement, generated by the database."
        },
        "detection_id": {
          "description": "Detection this measurement describes."
        },
        "extractor_feature_id": {
          "description": "Extractor-declared feature this is a measurement of, which is what makes the stored number interpretable. Required, because a value whose declared meaning is unknown is not a measurement."
        },
        "canonical_feature_id": {
          "description": "Canonical feature the measurement was read as, when a mapping declared one. Null when the quantity has no canonical counterpart, which leaves the native value intact and simply uninterpreted in VAWLUME's own vocabulary."
        },
        "source_artifact_id": {
          "description": "Artifact this value was read from."
        },
        "native_value_type": {
          "description": "Which of the native typed columns carries the extractor's value, or the declared absence of one. A constraint enforces that exactly one is populated, and that none is when the value is absent, so a measurement the extractor could not make is never confused with a zero."
        },
        "native_raw_token": {
          "description": "The original lexical token as it appeared in the export, before any parsing. Text even when it looks numeric, because it records what the source wrote rather than a number VAWLUME computed, and a sentinel or blank is preserved here as the explicit missingness it represents."
        },
        "native_value_real": {
          "description": "The extractor's value when it is a real number, and null otherwise."
        },
        "native_value_integer": {
          "description": "The extractor's value when it is an integer, and null otherwise."
        },
        "native_value_text": {
          "description": "The extractor's value when it is textual, and null otherwise."
        },
        "native_value_boolean": {
          "description": "The extractor's value when it is a truth value, stored as 0 or 1, and null otherwise."
        },
        "native_value_json": {
          "description": "The extractor's value when it is a structured payload, stored as JSON text, and null otherwise."
        },
        "native_unit": {
          "description": "Unit the extractor expressed this value in, as the extractor expressed it."
        },
        "canonical_value_real": {
          "description": "The canonicalized reading when it is a real number, and null otherwise. Present alongside the native value rather than instead of it."
        },
        "canonical_value_integer": {
          "description": "The canonicalized reading when it is an integer, and null otherwise."
        },
        "canonical_value_text": {
          "description": "The canonicalized reading when it is textual, and null otherwise."
        },
        "canonical_value_boolean": {
          "description": "The canonicalized reading when it is a truth value, stored as 0 or 1, and null otherwise."
        },
        "canonical_value_json": {
          "description": "The canonicalized reading when it is a structured payload, stored as JSON text, and null otherwise."
        },
        "canonical_unit": {
          "description": "Unit the canonicalized reading is expressed in, which may differ from the native unit when a transform was applied."
        },
        "transform_key": {
          "description": "Named transform that produced the canonical value from the native one, from a fixed whitelist. Null when no conversion was applied, including when the value was absent and therefore had nothing to convert."
        },
        "operational_variant": {
          "description": "Which variant of the quantity this measurement is, carried on the row so that a join selecting one canonical feature can still tell two operational definitions apart."
        },
        "source_locator": {
          "description": "Where in the source artifact this value was read from, recorded so a stored number can be traced back to the cell it came from."
        },
        "notes": {
          "description": "Free-text remarks about this measurement. Never parsed by VAWLUME."
        }
      }
    },
    "unmapped_source_values": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "A value present in an extractor's output that no mapping profile claimed, preserved row by row with where it came from. Retaining it is the point: an unrecognized column is evidence whose meaning is not yet mapped, not noise to be discarded.",
      "columns": {
        "unmapped_value_id": {
          "description": "VAWLUME surrogate key for this preserved value, generated by the database."
        },
        "extraction_run_id": {
          "description": "Run whose output the value appeared in. Null when the value is scoped more narrowly and the run is reachable through that scope instead."
        },
        "detection_id": {
          "description": "Detection the value belonged to, when it appeared on an event row. Null for a value that was not per-event."
        },
        "extractor_object_id": {
          "description": "Node of the extractor's hierarchy the value belonged to, when it appeared at container level rather than on an event."
        },
        "source_artifact_id": {
          "description": "Artifact the value was read from."
        },
        "native_field_name": {
          "description": "The column or field name as the extractor wrote it, preserved exactly, since that name is most of what a later reader has to work out what the value meant."
        },
        "raw_value_text": {
          "description": "The value exactly as it appeared, held as text because it was never parsed into a typed value. Nothing was inferred about what kind of quantity it is."
        },
        "native_unit": {
          "description": "Unit the source stated for this value, when it stated one. Recorded without being interpreted."
        },
        "source_locator": {
          "description": "Where in the source artifact the value was read from, so it can be traced back to the cell it came from."
        },
        "reason_unmapped": {
          "description": "Why no mapping claimed the value, such as a column the profile does not declare. Distinguishes a value nobody described from a value deliberately excluded."
        },
        "mapping_profile_version_id": {
          "description": "Profile version that was in force when the value went unclaimed, so a later revision of the profile can be checked against what it previously failed to describe."
        }
      }
    },
    "curation_events": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "A recorded act of review or state change on one detection, attributed to whoever performed it. Review state is evidence about a detection, never a filter applied to it: a detection marked rejected is still imported and still present.",
      "columns": {
        "curation_event_id": {
          "description": "VAWLUME surrogate key for this review record, generated by the database."
        },
        "detection_id": {
          "description": "Detection the review act concerns."
        },
        "source_artifact_id": {
          "description": "Artifact the review state was read from."
        },
        "action_type": {
          "description": "What was done to the detection, such as accepting or rejecting it, preserving the source's own term for the act."
        },
        "status_after": {
          "description": "State the detection was in once the act was recorded. Held separately from the act because the same act may leave different states in different tools' vocabularies."
        },
        "actor_type": {
          "description": "Who performed the act, distinguishing the extractor's own automatic state from a human reviewer, from VAWLUME itself, and from an unknown actor. An extractor's own review flag never becomes a human reviewer: no reviewer is invented from an export that states only a state."
        },
        "actor_label": {
          "description": "Identifier or name for the actor, when the source named one. Null when the source recorded only a state and not who set it."
        },
        "event_time_utc": {
          "description": "UTC time the review act occurred, when the source recorded one. Null when the export carries no such evidence."
        },
        "details_json": {
          "description": "Further structured detail about the act, preserved as JSON text where the schema has no dedicated column for it."
        },
        "notes": {
          "description": "Free-text remarks about the review act. Never parsed by VAWLUME."
        }
      }
    },
    "classification_runs": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "One labelling of an extraction run's detections, whether by a model, by clustering, or by hand. Kept separate from detection so that label evidence can be recorded without asserting that a label is a biological fact about the call.",
      "columns": {
        "classification_run_id": {
          "description": "VAWLUME surrogate key for this labelling, generated by the database."
        },
        "project_id": {
          "description": "Project the labelling belongs to."
        },
        "parent_extraction_run_id": {
          "description": "Extraction run whose detections are being labelled. A label assignment may only concern a detection from this run, which a trigger enforces."
        },
        "extractor_object_id": {
          "description": "Node of the extractor's hierarchy the labelling was scoped to, when it was scoped more narrowly than the whole run."
        },
        "model_artifact_id": {
          "description": "Registered model that produced the labels, when one was supplied. Null when the source states labels without saying what produced them, and a model is never inferred from the extractor's version or its defaults."
        },
        "settings_profile_version_id": {
          "description": "Profile version recording how the labelling was configured, when that was recoverable."
        },
        "method": {
          "description": "How the labels were arrived at. An export that states labels without saying whether they were manual, supervised, or clustering-derived is recorded as exactly that unstated provenance rather than assigned a plausible method."
        },
        "run_label": {
          "description": "Optional human-readable label for this labelling. Free text."
        },
        "number_of_classes": {
          "description": "How many classes the labelling distinguishes, when that was stated. Null when the source did not say."
        },
        "created_at_utc": {
          "description": "UTC time the labelling was produced, when the source recorded one."
        },
        "notes": {
          "description": "Free-text remarks about the labelling. Never parsed by VAWLUME."
        }
      }
    },
    "classification_classes": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "One class a labelling can assign, holding the source's own term for it and, separately, any canonical reading of that term. The native token is preserved exactly and acquires no biological meaning by being stored.",
      "columns": {
        "classification_class_id": {
          "description": "VAWLUME surrogate key for this class, generated by the database."
        },
        "classification_run_id": {
          "description": "Labelling this class belongs to. Classes are per labelling, because the same word may mean different things in two labellings."
        },
        "native_class_id": {
          "description": "The source's own identifier for the class, unique within its labelling."
        },
        "native_class_label": {
          "description": "The source's own name for the class, preserved exactly as written."
        },
        "canonical_class_label": {
          "description": "VAWLUME's reading of what the class corresponds to, when a caller declared one. Null when the source's label has been preserved without any canonical meaning being claimed for it, which is the ordinary case for an imported label of unstated provenance."
        },
        "description": {
          "description": "Free-text account of what the class is taken to mean. Never parsed by VAWLUME."
        }
      }
    },
    "classification_assignments": {
      "kind": "table",
      "domain": "detections_curation",
      "description": "Records that one labelling assigned one class to one detection, with the score behind it where the source gave one. At most one assignment per detection per labelling, so a labelling states one answer rather than several.",
      "columns": {
        "classification_assignment_id": {
          "description": "VAWLUME surrogate key for this assignment, generated by the database."
        },
        "detection_id": {
          "description": "Detection being labelled. Must belong to the labelling's parent extraction run, which a trigger enforces."
        },
        "classification_run_id": {
          "description": "Labelling that made this assignment."
        },
        "classification_class_id": {
          "description": "Class assigned. Must be one of the classes belonging to this same labelling, which a trigger enforces."
        },
        "score_or_distance": {
          "description": "The number behind the assignment, which may be a confidence or a distance depending on the method. On the source's own scale and uncalibrated; its direction of goodness is a property of the method rather than of this column."
        },
        "assignment_source": {
          "description": "Who or what made the assignment, defaulting to the extractor. Distinguishes a label imported from an export from one added afterwards."
        },
        "notes": {
          "description": "Free-text remarks about the assignment. Never parsed by VAWLUME."
        }
      }
    },
    "analysis_runs": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "One execution of a VAWLUME analysis over material already in the database, such as matching two extraction runs or composing agreement across several. Analyses are immutable: a changed specification or algorithm produces a new run rather than altering what an existing one concluded.",
      "columns": {
        "analysis_run_id": {
          "description": "VAWLUME surrogate key for the run, generated by the database. Everything an analysis derives cites this, which is what keeps one analysis's conclusions separable from another's over the same detections."
        },
        "project_id": {
          "description": "Project the analysis belongs to. Its run key is unique within the project rather than globally."
        },
        "parent_analysis_run_id": {
          "description": "Analysis this one is a child of, for a derivation with a single genuine parent such as the agreement-statistics run hanging off a matching analysis. Null for a run with no single parent, including one composed from several sources, whose lineage is recorded in its own relation rather than forced into this column."
        },
        "run_type": {
          "description": "What kind of analysis this is, such as pairwise matching, cross-extractor agreement statistics, or arbitrary-N agreement composition. An open vocabulary rather than a constrained one, though some values carry enforced consequences: agreement groups may only hang off a run declaring the arbitrary-N agreement type."
        },
        "run_key": {
          "description": "Caller-supplied stable identifier for the analysis, unique within its project. Where a change of algorithm or specification would change what the run means, the convention is to build that version into the key, so a rerun under new assumptions becomes a new run rather than silently redefining an old one."
        },
        "run_label": {
          "description": "Optional human-readable label for the analysis. Free text with no uniqueness requirement."
        },
        "vawlume_version": {
          "description": "Version of VAWLUME the caller asserted was in use. Supplied on the run specification and never inferred from the working tree, so it records a claim rather than a verified fact."
        },
        "source_commit": {
          "description": "Source revision the caller asserted was in use. Like the version, a caller claim VAWLUME records without checking."
        },
        "status": {
          "description": "State of the analysis, drawn from a declared vocabulary that separates one still in progress from one that completed, one that completed with warnings, and one that failed. Derived rows are written inside the transaction that completes a run, so a completed run's graph is whole."
        },
        "started_at_utc": {
          "description": "UTC timestamp recorded when the analysis began."
        },
        "completed_at_utc": {
          "description": "UTC timestamp recorded when the analysis finished. Null while it is unfinished or if it never reached a terminal state."
        },
        "notes": {
          "description": "Free-text remarks about the analysis. Never parsed by VAWLUME."
        }
      }
    },
    "analysis_run_profiles": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records that a configuration profile version governed an analysis in a named role, which is how a stored conclusion stays attributable to the specification that produced it.",
      "columns": {
        "analysis_run_id": {
          "description": "Analysis the profile version governed."
        },
        "profile_version_id": {
          "description": "Exact profile version in force, including its checksum-bearing content. A conclusion is only reproducible if the rules behind it can be recovered exactly."
        },
        "assignment_role": {
          "description": "The role this profile version played for the analysis, such as the matching specification or the agreement specification. Part of the assignment's identity, so one version can govern a run in more than one role."
        }
      }
    },
    "analysis_run_extraction_inputs": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Declares which extraction runs an analysis was permitted to draw detections from. This is the boundary that makes an analysis's population explicit: a group may only contain detections from a run declared here, and a trigger enforces it.",
      "columns": {
        "analysis_run_id": {
          "description": "Analysis consuming the extraction run."
        },
        "extraction_run_id": {
          "description": "Extraction run whose detections the analysis may consider."
        },
        "input_role": {
          "description": "How the analysis used this input. Part of the declaration's identity, so one extraction run can be supplied to an analysis in more than one role."
        }
      }
    },
    "analysis_run_sources": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records that one analysis was composed from another. An analysis over three extractors consumes three pairwise analyses and has no single parent among them, so multi-source lineage lives here rather than being forced into a single parent column.",
      "columns": {
        "analysis_run_id": {
          "description": "Derived analysis, the one that consumed the other."
        },
        "source_analysis_run_id": {
          "description": "Analysis that was consumed, which may not be the derived analysis itself. A cited source cannot be deleted while the derived analysis still stands, because a stored conclusion must not outlive the evidence it was composed from."
        },
        "dependency_role": {
          "description": "How the source contributed to the derived analysis. Descriptive rather than part of identity: one source contributes to one derived analysis once, so the pair alone identifies the dependency."
        },
        "notes": {
          "description": "Free-text remarks about this dependency. Never parsed by VAWLUME."
        }
      }
    },
    "candidate_pairs": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "One pair of detections from different extraction runs that an analysis considered as possibly the same event, together with the temporal measures that informed the decision. A candidate is evidence that two detections were compared, not a claim that they correspond; the conclusion is recorded separately.",
      "columns": {
        "candidate_pair_id": {
          "description": "VAWLUME surrogate key for the candidate, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis that considered this pair. The pair is unique within its analysis, so two analyses may reach different conclusions about the same two detections."
        },
        "recording_id": {
          "description": "Recording both detections belong to. A trigger refuses a pair whose detections do not both come from this recording."
        },
        "detection_a_id": {
          "description": "One endpoint of the pair, always the lower of the two identifiers. The ordering is by identifier rather than by run or role, and exists so one conceptual pair cannot be stored twice in opposite directions; neither endpoint is privileged and neither denotes a particular extractor."
        },
        "detection_b_id": {
          "description": "The other endpoint, always the higher identifier. As with the a endpoint, the position carries ordering rather than meaning, and a consumer that needs to know which extractor a side came from resolves it through the detection rather than from this column."
        },
        "temporal_overlap_s": {
          "description": "Seconds the two detections overlap in the recording's own timebase. Zero-length overlap is not a candidate: the analysis considers a pair only where overlap is positive."
        },
        "temporal_iou": {
          "description": "Intersection over union of the two intervals, between zero and one. The measure the candidate threshold is applied to, and uncalibrated: the floor it is compared against is declared in the specification and is illustrative rather than validated."
        },
        "onset_difference_s": {
          "description": "Difference between the two detections' onsets, in seconds. Stored alongside the other measures rather than combined into a single score, because the layer reports several transparent measures instead of one number."
        },
        "offset_difference_s": {
          "description": "Difference between the two detections' offsets, in seconds."
        },
        "duration_difference_s": {
          "description": "Difference between the two detections' durations, in seconds."
        },
        "candidate_score": {
          "description": "The specification's own score for the candidate, whose definition travels with that specification rather than being fixed by this column. Uncalibrated, and deliberately not a blend of the separate measures above: the layer reports several transparent measures rather than combining them into one number that would read like a probability."
        },
        "candidate_status": {
          "description": "What the analysis concluded about the pair, defaulting to its being a candidate. Records the pair's standing within the analysis rather than a judgement about the underlying events."
        },
        "details_json": {
          "description": "Further structured detail about how the pair was evaluated, preserved as JSON text where the schema has no dedicated column for it."
        }
      }
    },
    "match_groups": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "A connected component of detections that one pairwise analysis grouped together for one recording, with the topology that membership implies. Groups come from graph connectivity over candidate edges: there is no optimizer, no score winner, and no forced one-to-one assignment.",
      "columns": {
        "match_group_id": {
          "description": "VAWLUME surrogate key for the group, generated by the database. A storage identity: the group's scientific identity for rerun comparison is its ordered membership, not this number."
        },
        "analysis_run_id": {
          "description": "Analysis that formed this group. Every detection in the analysis's inputs belongs to exactly one of its groups, which a trigger enforces."
        },
        "recording_id": {
          "description": "Recording the grouped detections belong to. Grouping is per recording."
        },
        "match_type": {
          "description": "Topology of the group, from a declared vocabulary covering one-to-one, one-to-many, many-to-one, many-to-many, an explicitly unmatched single member, and an ambiguous case. Derived from member counts on the ordered run pair alone. Split and merge readings of the uneven topologies are relative descriptions of what two runs did, never claims about what happened biologically."
        },
        "ambiguity_status": {
          "description": "Whether the topology leaves the correspondence unambiguous, ambiguous, or unmatched. Held separately from the topology because it is the consequence a consumer usually needs, and because ambiguity outranks feature evidence in every downstream judgement."
        },
        "match_score": {
          "description": "Score for the group as a whole. Left null by the current matching implementation, which specifies no component score; a number here would have to be defined before it could be read as anything."
        },
        "notes": {
          "description": "Free-text remarks about the group, carrying the stable membership key and member counts that constitute the group's rerun identity. Never parsed by VAWLUME."
        }
      }
    },
    "match_group_members": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records that one detection belongs to one match group. Membership is a partition within an analysis: a detection appears in exactly one group of a given analysis, and in as many analyses as have considered it.",
      "columns": {
        "match_group_id": {
          "description": "Group the detection belongs to."
        },
        "detection_id": {
          "description": "Detection that belongs to it. Must come from one of the analysis's declared extraction inputs and must not already belong to another group of the same analysis; triggers enforce both."
        },
        "member_role": {
          "description": "Which side of the ordered run pair this detection came from, where the group's topology needs the sides distinguished."
        }
      }
    },
    "consensus_events": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "An interval derived from a match group's members under a declared boundary rule: VAWLUME's own reading of when the event occurred, given that several extractors reported it. Derived rather than observed, and emitted only for topologies the specification permits — a many-to-many or unmatched group yields none.",
      "columns": {
        "consensus_event_id": {
          "description": "VAWLUME surrogate key for the derived event, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis that derived this event."
        },
        "match_group_id": {
          "description": "Group the event was derived from, whose membership it repeats exactly. Null when the group it came from is no longer present, which leaves the derived interval without its lineage rather than removing it."
        },
        "recording_id": {
          "description": "Recording the derived interval falls in."
        },
        "start_time_s": {
          "description": "Derived onset in seconds from the recording's start, computed from member detections under the declared boundary rule. A VAWLUME product, not an extractor observation, and it never overwrites any member's native timing."
        },
        "end_time_s": {
          "description": "Derived offset in seconds, never before the derived onset, computed under the same boundary rule."
        },
        "derivation_method": {
          "description": "Which boundary rule produced the interval, such as the mean of the members' boundaries or their union. Recorded because the two answer different questions: a mean speaks for an unambiguous correspondence, a union preserves the extent of an ambiguous one."
        },
        "consensus_status": {
          "description": "Whether the derivation was over an unambiguous correspondence or one whose ambiguity the boundary rule preserved. Distinguishes an interval two extractors agreed on from one that spans a split or merge without resolving it."
        },
        "confidence_score": {
          "description": "Confidence in the derived event. Left null by the current implementation, which defines no calibrated confidence model; a number here would need one before it could be interpreted."
        },
        "notes": {
          "description": "Free-text remarks about the derived event. Never parsed by VAWLUME."
        }
      }
    },
    "consensus_event_members": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records which detections a consensus event was derived from, repeating its match group's membership exactly. Kept as its own relation so a derived interval carries its evidence rather than pointing at a group a reader must then resolve.",
      "columns": {
        "consensus_event_id": {
          "description": "Derived event the detection contributed to."
        },
        "detection_id": {
          "description": "Detection that contributed."
        },
        "member_role": {
          "description": "Which side of the ordered run pair this detection came from, carried over from the match group."
        }
      }
    },
    "consilience_assessments": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "One automated categorical judgement about a match group or a consensus event, assigned by a frozen rule set carried in a versioned specification. A summary of what evidence was available under one set of assumptions, never a verdict on whether a vocalization occurred.",
      "columns": {
        "consilience_assessment_id": {
          "description": "VAWLUME surrogate key for the assessment, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis that produced the assessment, and through which the specification that defined the rules can be recovered."
        },
        "match_group_id": {
          "description": "Match group assessed, when the assessment targets a group. Exactly one of the group and the event references is populated, which a constraint enforces, so an assessment always has one unambiguous subject."
        },
        "consensus_event_id": {
          "description": "Consensus event assessed, when the assessment targets a derived event instead. The alternative to the group reference, never populated alongside it."
        },
        "status": {
          "description": "The automated conclusion, such as a correspondence supported by feature evidence, one supported only temporally, one whose features disagreed, one whose topology was ambiguous, or a detection no other extractor corroborated. The last of those means only that no eligible cross-extractor correspondence survived this specification; it is not a claim that the detection was false. A manual verdict never overwrites this column, so it always reports what the algorithm decided."
        },
        "score": {
          "description": "Numeric score for the assessment. Left null by the current implementation, deliberately: the status is a categorical evidence summary under one rule set, and no calibration exists that would justify reading a number here as a probability that an event occurred."
        },
        "rationale_json": {
          "description": "The evidence behind the status, preserved as JSON text: the rule set and its version and checksum, the topology, the rule text that produced the status, which feature comparisons were available, within tolerance, outside it, or not computable, and the temporal evidence read from the candidate. What makes the status auditable rather than merely asserted."
        },
        "assessed_at_utc": {
          "description": "UTC timestamp recorded when the assessment was written. An audit time."
        }
      }
    },
    "manual_reviews": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "A human verdict on something VAWLUME already holds: a detection, a match group, or a consensus event. Independent evidence, never derived from extractor state, and it never overwrites an automated status; where the two disagree, both remain readable and the resolution is computed rather than stored.",
      "columns": {
        "manual_review_id": {
          "description": "VAWLUME surrogate key for the review, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis the review was made in the context of. Null for a review not tied to one, and set to null rather than removed if that analysis is deleted, because the human judgement outlives the run that prompted it."
        },
        "detection_id": {
          "description": "Detection reviewed, when the review targets one. Exactly one of the three subject references is populated, which a constraint enforces."
        },
        "match_group_id": {
          "description": "Match group reviewed, when the review targets a correspondence rather than a single detection."
        },
        "consensus_event_id": {
          "description": "Consensus event reviewed, when the review targets a derived interval."
        },
        "reviewer_label": {
          "description": "Identifier for the person or protocol that made the verdict. Recorded so reviews can be attributed and compared, not to establish authority."
        },
        "review_status": {
          "description": "The human verdict, from a sub-vocabulary separate from the automated statuses. Adjudication values belong here and never appear in the automated status column, which is what keeps the algorithm's conclusion and the reviewer's distinguishable."
        },
        "corrected_start_time_s": {
          "description": "Onset the reviewer considers correct, in seconds from the recording's start. Recorded as the reviewer's assertion alongside the original; no native or derived timing is rewritten by it."
        },
        "corrected_end_time_s": {
          "description": "Offset the reviewer considers correct, never before the corrected onset. Like the onset, an assertion held beside the original rather than replacing it."
        },
        "reviewed_at_utc": {
          "description": "UTC timestamp recorded when the review was written. An audit time."
        },
        "notes": {
          "description": "Free-text remarks from the reviewer. Never parsed by VAWLUME."
        }
      }
    },
    "manual_reference_events": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "An event a reviewer marked in a recording, belonging to a named reference set rather than to any analysis. Scoped this way on purpose: a reference set anchors every configuration compared against it, and it can hold an event no extractor found, which is what makes recall and false negatives representable at all.",
      "columns": {
        "manual_reference_event_id": {
          "description": "VAWLUME surrogate key for the reference event, generated by the database."
        },
        "recording_id": {
          "description": "Recording the reviewer marked the event in. Reference events are scoped to a recording and a set, never to an analysis run, so one reviewed subset can anchor comparisons across many configurations."
        },
        "reference_set_key": {
          "description": "Name of the reviewed subset this event belongs to. Whether the set covers a whole recording or only part of it is declared in the specification that consumes it, because that is a property of how the review was conducted rather than something the schema can know."
        },
        "native_reference_id": {
          "description": "The reviewer's own identifier for the event, preserved as expressed. Together with the recording and the set it forms the event's durable identity."
        },
        "reviewer_label": {
          "description": "Identifier for the person or protocol that marked the event."
        },
        "start_time_s": {
          "description": "Onset the reviewer marked, in seconds from the recording's start."
        },
        "end_time_s": {
          "description": "Offset the reviewer marked, never before the onset."
        },
        "event_status": {
          "description": "Whether the reviewer marked the event plainly or flagged it as uncertain. The uncertainty is the reviewer's own and is preserved rather than resolved, so a comparison can choose whether to count it."
        },
        "reviewed_at_utc": {
          "description": "UTC timestamp recorded when the reference event was written. An audit time, not when the reviewer looked at the audio."
        },
        "notes": {
          "description": "Free-text remarks from the reviewer about this event. Never parsed by VAWLUME."
        }
      }
    },
    "agreement_statistics": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "An aggregate summarizing how far two extraction runs agreed, under one analysis. Belongs to the pairwise consilience layer despite its general name: the arbitrary-N agreement layer stores no summary at all, because its counts and patterns are computed from member and edge rows so they cannot drift from the evidence. Only aggregates live here; the per-comparison values behind them are recomputable and deliberately not cached.",
      "columns": {
        "agreement_statistic_id": {
          "description": "VAWLUME surrogate key for the aggregate, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis this aggregate belongs to, ordinarily a child run whose parent is the matching analysis being summarized and which cites the same specification and ordered inputs, so the aggregate is self-describing."
        },
        "statistic_kind": {
          "description": "Which family the aggregate belongs to, from a declared vocabulary separating agreement about which events were detected, agreement about measured feature values, diagnostics about the matching itself, and anything else. The family determines which of the optional references below are meaningful."
        },
        "feature_a_id": {
          "description": "One of the two registered extractor features a feature-level aggregate concerns. Populated together with the other endpoint so the aggregate stays traceable to the exact registered pair it summarizes; null for an aggregate that is not about a feature pair. Unlike the endpoints of a candidate pair, these carry no enforced ordering."
        },
        "feature_b_id": {
          "description": "The other registered extractor feature of the pair a feature-level aggregate concerns. Null for an aggregate that is not about a feature pair."
        },
        "scope_entity_id": {
          "description": "Experimental entity the aggregate was computed within, when it was scoped more narrowly than the whole analysis. Null for an aggregate over everything the analysis covered."
        },
        "statistic_name": {
          "description": "What quantity this row reports, within its family. The name identifies the measure; its denominator, direction, and exact definition travel with the row rather than being inferred from the name."
        },
        "statistic_value": {
          "description": "The value of the reported quantity. On the scale its own definition establishes and uncalibrated, so it describes what two runs did under one specification rather than how well either performed."
        },
        "lower_bound": {
          "description": "Lower end of an interval around the value, where the measure defines one. Null where it does not, and not to be read as a confidence interval unless the row's own definition says so."
        },
        "upper_bound": {
          "description": "Upper end of an interval around the value, where the measure defines one."
        },
        "n_observations": {
          "description": "How many observations the aggregate was computed over. The denominator a value must be read against: a high agreement rate over three events and over three hundred are different claims."
        },
        "notes": {
          "description": "Structured detail carrying the aggregate's denominator, direction, scope, and definition. Preserved as text because those facts must travel with the number rather than being reconstructed from its name."
        }
      }
    },
    "agreement_groups": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "A component of detections that an arbitrary-N agreement run composed for one recording from the exact candidate edges of two or more pairwise analyses. Pairwise correspondence remains the primitive; this layer composes those results without replacing them, and may only hang off a run declaring the agreement type.",
      "columns": {
        "agreement_group_id": {
          "description": "VAWLUME surrogate key for the component, generated by the database."
        },
        "analysis_run_id": {
          "description": "Agreement run that composed this component. A trigger refuses a group whose run is not of the arbitrary-N agreement type, so composed results cannot be grafted onto a pairwise matching analysis."
        },
        "recording_id": {
          "description": "Recording the component's detections belong to. Composition is per recording, and the recording must belong to the run's project."
        },
        "group_key": {
          "description": "Stable identity of the component, built from the sorted list of its members' selectors, and unique within its run. This rather than the surrogate key is what makes two runs' components comparable."
        },
        "derivation_method": {
          "description": "How the component was composed from the source analyses' edges. Recorded because the composition rule is a choice, and a component means something different under a different one."
        },
        "notes": {
          "description": "Free-text remarks about the component. Never parsed by VAWLUME."
        }
      }
    },
    "agreement_group_members": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records that one detection belongs to one composed agreement component. A detection belongs to exactly one component of a given agreement run, and may belong to a component in as many agreement runs as have considered it.",
      "columns": {
        "agreement_group_id": {
          "description": "Component the detection belongs to."
        },
        "detection_id": {
          "description": "Detection that belongs to it, which must come from the run's declared extraction inputs and must not already belong to another component of the same run. The detection cannot be deleted while the component holds it: a member joined to the component by no candidate edge would otherwise vanish silently, leaving a component whose stored key still named it."
        },
        "member_role": {
          "description": "How this detection sits within the component, where the members need distinguishing."
        }
      }
    },
    "agreement_supporting_edges": {
      "kind": "table",
      "domain": "matching_agreement",
      "description": "Records that one pairwise candidate edge is part of what holds a composed component together. Citing the exact edge rather than a count is the point: the evidence for a composed component stays inspectable, and summary quantities are computed from these rows instead of being stored beside them.",
      "columns": {
        "agreement_supporting_edge_id": {
          "description": "VAWLUME surrogate key for the citation, generated by the database."
        },
        "agreement_group_id": {
          "description": "Composed component this edge supports."
        },
        "candidate_pair_id": {
          "description": "Exact pairwise candidate edge being cited, which must come from one of the run's declared source analyses, describe the same recording, and join two detections that are both members of this component. The cited candidate cannot be deleted while the citation stands. Whether a citation is recorded never depends on feature support or consilience status: an edge is cited for joining two members, and what the features said is a separate question."
        },
        "notes": {
          "description": "Free-text remarks about this citation. Never parsed by VAWLUME."
        }
      }
    },
    "timebases": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One declared clock coordinate system, such as a recording sample clock, video frame clock, controller clock, or acquisition-system clock. Being an alignment reference is a property of an alignment set, not of this row.",
      "columns": {
        "timebase_id": {
          "description": "VAWLUME surrogate key for the declared clock, generated by the database."
        },
        "project_id": {
          "description": "Project in which this clock is declared."
        },
        "recording_id": {
          "description": "Recording this clock belongs to, when it is recording-scoped. Null for a project-scoped clock that may be shared by several streams."
        },
        "timebase_name": {
          "description": "Caller-supplied name for the clock, unique within its recording when recording-scoped and otherwise within its project."
        },
        "timebase_kind": {
          "description": "Free-text description of the kind of clock. VAWLUME deliberately imposes no acquisition-device vocabulary on this field."
        },
        "is_recording_native": {
          "description": "Whether this is the single clock in which the recording's detections are expressed (1) or not (0). A native recording clock must name a recording."
        },
        "native_unit": {
          "description": "Unit in which values on this clock are expressed, as declared for the timebase."
        },
        "nominal_rate_hz": {
          "description": "Declared nominal sampling or frame rate in hertz. Null when no rate was supplied; absence is not a claim of zero rate."
        },
        "origin_description": {
          "description": "Free-text account of what native time zero means for this clock. Null when the origin was not described."
        },
        "clock_identifier": {
          "description": "Source- or device-facing identifier for the clock, when one was supplied. Null when the source exposes no separate identifier."
        },
        "notes": {
          "description": "Free-text remarks about the declared clock. Never parsed by VAWLUME."
        }
      }
    },
    "external_streams": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One logical stream of external records on a declared clock, such as the behaviour scoring for a session. Files supplying the stream are recorded separately so one stream may span or survive several exports.",
      "columns": {
        "external_stream_id": {
          "description": "VAWLUME surrogate key for the logical stream, generated by the database."
        },
        "project_id": {
          "description": "Project that owns this logical stream."
        },
        "recording_id": {
          "description": "Recording this stream accompanies, when it is recording-scoped. Null for a project-scoped stream."
        },
        "timebase_id": {
          "description": "Clock in whose native coordinates this stream's events and coverage intervals are stated."
        },
        "stream_name": {
          "description": "Caller-supplied name for the logical stream, unique within its recording when recording-scoped and otherwise within its project."
        },
        "stream_kind": {
          "description": "Operational category separating event, continuous, annotation, video, TTL, tracking, and other streams without treating modality as clock identity."
        },
        "modality": {
          "description": "Free-text modality label supplied for the stream. Null when no modality was declared."
        },
        "units": {
          "description": "Declared units for values carried by the stream, when one stream-level unit applies. Null when units are absent or belong on individual events or attributes."
        },
        "notes": {
          "description": "Free-text remarks about the logical stream. Never parsed by VAWLUME."
        }
      }
    },
    "external_stream_sources": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "Records one provenance source that supplied records for a logical external stream. Each row names exactly one registered source file or artifact, and several rows may compose one stream.",
      "columns": {
        "external_stream_source_id": {
          "description": "VAWLUME surrogate key for this stream-source citation, generated by the database."
        },
        "external_stream_id": {
          "description": "Logical external stream whose records came from the cited source."
        },
        "source_file_id": {
          "description": "Registered source file that supplied the records. Null when the row cites an artifact instead; exactly one of the two source references is present."
        },
        "artifact_id": {
          "description": "Registered artifact that supplied the records. Null when the row cites a source file instead; exactly one of the two source references is present."
        },
        "source_role": {
          "description": "Role the cited source plays for the stream, distinguishing event, anchor, coverage, attribute, and other records."
        },
        "mapping_profile_version_id": {
          "description": "Configuration-profile version used to interpret this source, when one was registered. Null means no mapping-profile version is cited."
        },
        "source_locator": {
          "description": "Location within the cited file or artifact from which these stream records came, when the source exposes one."
        },
        "notes": {
          "description": "Free-text remarks about this provenance citation. Never parsed by VAWLUME."
        }
      }
    },
    "external_events": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One event observed in a logical external stream, retaining the source's native identity, label, and timestamps beside an operational event key. Native time is evidence and is never rewritten during alignment.",
      "columns": {
        "external_event_id": {
          "description": "VAWLUME surrogate key for the external event, generated by the database."
        },
        "external_stream_id": {
          "description": "Logical stream in which this event was observed and whose timebase gives its native clock."
        },
        "entity_id": {
          "description": "Experimental entity the source declared this event concerned, when that label resolved to an existing entity. Null means no declared link resolved; this lookup is not visual-identity evidence."
        },
        "native_event_id": {
          "description": "Identifier supplied by the source for this event, unique within the stream when present. Null when the source supplied none; VAWLUME does not invent one."
        },
        "native_event_label": {
          "description": "Event label preserved from the source's own vocabulary. Null when the source supplied no native label."
        },
        "event_type": {
          "description": "Normalized operational key VAWLUME queries for this event. It may repeat the native label when no mapping rule supplies a different key and is not a universal behavioural or neural taxonomy."
        },
        "start_time_native": {
          "description": "Event start in the external stream's native clock. Alignment never updates this value; projected time is derived from a stored transform."
        },
        "end_time_native": {
          "description": "Event end in the external stream's native clock. Null for a point event; when present it cannot precede the native start and is never overwritten by alignment."
        },
        "value_text": {
          "description": "Text-valued scalar carried by the event, when the mapped event has one. Null when absent or when its scalar value is numeric."
        },
        "value_real": {
          "description": "Numeric scalar carried by the event, when the mapped event has one. Null when absent or when its scalar value is text."
        },
        "unit": {
          "description": "Declared unit for the event's scalar value, when one was supplied. Null means the value has no recorded unit, not that it is dimensionless."
        },
        "mapping_rule_key": {
          "description": "Key of the mapping rule that produced the operational event representation, when the profile records one."
        },
        "source_locator": {
          "description": "Location of this event within its source, such as a row or record locator, when one was retained."
        },
        "notes": {
          "description": "Free-text remarks about the external event. Never parsed by VAWLUME."
        }
      }
    },
    "external_event_attributes": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One named long-form attribute mapped from an external event. The row preserves the native field and token while storing exactly one typed value, or an explicit missing-value statement.",
      "columns": {
        "external_event_attribute_id": {
          "description": "VAWLUME surrogate key for the event attribute, generated by the database."
        },
        "external_event_id": {
          "description": "External event this mapped attribute describes."
        },
        "attribute_name": {
          "description": "Canonical attribute name assigned by the mapping profile, unique within the event."
        },
        "native_field_name": {
          "description": "Field name used by the source before mapping. Null when the source did not expose a distinct field name."
        },
        "value_type": {
          "description": "Declared representation of the mapped value, separating text, real, integer, boolean, JSON, and explicit missingness."
        },
        "value_text": {
          "description": "Text payload when value_type is text. Null for every other representation and for an explicit missing value."
        },
        "value_real": {
          "description": "Real-number payload when value_type is real. Null for every other representation and for an explicit missing value."
        },
        "value_integer": {
          "description": "Integer payload when value_type is integer. Null for every other representation and for an explicit missing value."
        },
        "value_boolean": {
          "description": "Boolean payload encoded as 1 or 0 when value_type is boolean. Null for every other representation and for an explicit missing value."
        },
        "value_json": {
          "description": "JSON text payload when value_type is json. Null for every other representation and for an explicit missing value."
        },
        "native_raw_token": {
          "description": "Verbatim source token retained before type conversion, including a source missing-value token when one occurred. Null when no raw token was retained."
        },
        "unit": {
          "description": "Declared unit for this attribute value, when supplied by the mapping. Null means no unit was recorded."
        },
        "source_locator": {
          "description": "Location of this attribute within the source, when a row, field, or record locator was retained."
        },
        "mapping_rule_key": {
          "description": "Key of the mapping rule that interpreted this native field, when the profile records one."
        }
      }
    },
    "external_stream_coverage": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One profile-declared interval during which an external stream was observed, stated on that stream's native clock. Outside all such intervals availability is unknown, not evidence that no event occurred.",
      "columns": {
        "external_stream_coverage_id": {
          "description": "VAWLUME surrogate key for the coverage interval, generated by the database."
        },
        "external_stream_id": {
          "description": "Logical stream whose observation coverage this interval asserts."
        },
        "segment_index": {
          "description": "One-based position of this coverage segment within the stream's declared coverage."
        },
        "start_time_native": {
          "description": "Start of the observed interval in the stream's native clock. This source coordinate is never rewritten by alignment."
        },
        "end_time_native": {
          "description": "End of the observed interval in the stream's native clock; it cannot precede the native start and is never rewritten by alignment."
        },
        "observation_status": {
          "description": "Stream-level assertion about observation during this interval. The current vocabulary records observed intervals only; it does not describe transform support or extrapolation."
        },
        "source_locator": {
          "description": "Location in the source or profile from which this coverage assertion came, when one was retained."
        },
        "notes": {
          "description": "Free-text remarks about the declared coverage interval. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_sets": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One user-facing alignment operation that chooses a reference clock and groups the pairwise source-to-reference fits for a session. The reference is a coordinate choice, not a claim that its device is intrinsically correct.",
      "columns": {
        "alignment_set_id": {
          "description": "VAWLUME surrogate key for the alignment set, generated by the database."
        },
        "analysis_run_id": {
          "description": "Analysis run whose provenance and durable run identity belong to this alignment operation. Each analysis run can own at most one alignment set."
        },
        "recording_id": {
          "description": "Recording whose participating clocks are being aligned, when the set is recording-scoped. Null when no single recording scopes the set."
        },
        "reference_timebase_id": {
          "description": "Clock chosen as the coordinate system in which this set expresses other clocks. The choice does not assert that this clock is more accurate."
        },
        "manifest_source_file_id": {
          "description": "Registered source file containing the session-specific alignment manifest, when the manifest was file-backed. Null when absent or when an artifact is cited instead."
        },
        "manifest_artifact_id": {
          "description": "Registered artifact containing the session-specific alignment manifest, when artifact-backed. Null when absent or when a source file is cited instead."
        },
        "alignment_set_key": {
          "description": "Caller-supplied identifier for this alignment operation, when one was supplied. Null while no external key has been declared."
        },
        "status": {
          "description": "Lifecycle state of the set, distinguishing assembly, completed fitting, an explicit validation or rejection state, and failure. Current fitting moves a fully estimated set to fitted and never declares it validated."
        },
        "notes": {
          "description": "Free-text remarks about the alignment operation. Never parsed by VAWLUME."
        }
      }
    },
    "time_alignment_runs": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One attempt to express a source clock in its parent alignment set's reference clock. The row records the declared model and fit summaries; segment coefficients remain the transform authority.",
      "columns": {
        "alignment_run_id": {
          "description": "VAWLUME surrogate key for the pairwise transform attempt, generated by the database."
        },
        "alignment_set_id": {
          "description": "Alignment set that owns this source-to-reference transform attempt."
        },
        "source_timebase_id": {
          "description": "Source clock whose native values the stored transform accepts. It must differ from the target clock."
        },
        "target_timebase_id": {
          "description": "Reference clock in which transformed values are expressed. A trigger requires it to equal the parent alignment set's reference, so it is a readable convenience rather than a second authority."
        },
        "method": {
          "description": "Declared transform family: fixed-scale offset, affine, or continuous piecewise affine over caller-declared breakpoints. The method is not inferred from residuals."
        },
        "n_anchors_used": {
          "description": "Number of logical anchors used by the stored fit. On a registered but unfitted run, the current intake records the number then fit-eligible; null means no count was recorded, not zero."
        },
        "fit_rmse_s": {
          "description": "Root-mean-square residual in seconds across included anchors for the estimated fit. Null before or without a fit; the reported value is not an acceptance threshold or verdict."
        },
        "max_error_s": {
          "description": "Largest absolute residual in seconds across included anchors for the estimated fit. Null before or without a fit; it is reported evidence, not a pass/fail criterion."
        },
        "status": {
          "description": "State of this transform attempt, separating registered, estimated, validated, rejected, and failed. Current fitting records successful solutions as estimated and has no rule that produces validated."
        },
        "failure_code": {
          "description": "Machine-readable reason a transform failed, or a reason attached to a rejection. Null for registered or estimated runs and for a rejection with no coded cause."
        },
        "failure_reason": {
          "description": "Human-readable explanation of a failed or rejected outcome. It is required whenever failure_code is present; null means no explanation was recorded."
        },
        "notes": {
          "description": "Free-text remarks about the transform attempt. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_run_breakpoints": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One caller-declared source-clock breakpoint belonging to a piecewise-affine transform. VAWLUME persists the declaration for reproducibility and never estimates a breakpoint from residuals.",
      "columns": {
        "alignment_run_breakpoint_id": {
          "description": "VAWLUME surrogate key for the breakpoint declaration, generated by the database."
        },
        "alignment_run_id": {
          "description": "Piecewise-affine transform attempt whose source clock this breakpoint partitions."
        },
        "breakpoint_index": {
          "description": "One-based order of the breakpoint within the transform's declared set. Application validation, not this column alone, requires the set to increase monotonically."
        },
        "source_time": {
          "description": "Breakpoint coordinate on the transform's source clock. It is declared input, not a timestamp estimated by VAWLUME."
        },
        "declared_by": {
          "description": "Free-text indication of how the breakpoint declaration arrived, currently supplied by the caller rather than inferred by fitting."
        },
        "notes": {
          "description": "Free-text remarks about the breakpoint declaration. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_anchors": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One logical synchronization occurrence within an alignment set. It identifies what coordinated the clocks but stores no timestamp; separate observations record how each clock witnessed it.",
      "columns": {
        "alignment_anchor_id": {
          "description": "VAWLUME surrogate key for the logical synchronization anchor, generated by the database."
        },
        "alignment_set_id": {
          "description": "Alignment set within which this coordinating occurrence has its identity."
        },
        "anchor_key": {
          "description": "Caller-supplied identity for the logical anchor, unique within the alignment set and used to pair observations across clocks without nearest-time matching."
        },
        "anchor_type": {
          "description": "Free-text description of the kind of synchronization occurrence, such as a pulse or marker, without turning a nearby experimental event into the anchor itself."
        },
        "expected_order": {
          "description": "Caller-declared one-based expected position of this anchor in an ordered series, when supplied. Null means no expected order was declared."
        },
        "notes": {
          "description": "Free-text remarks about the logical anchor. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_anchor_observations": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One witnessing of a logical synchronization anchor on one timebase. Replicate readings remain separate QC evidence; at most one observation per anchor and clock may be included in a fit.",
      "columns": {
        "anchor_observation_id": {
          "description": "VAWLUME surrogate key for this clock reading of an anchor, generated by the database."
        },
        "alignment_anchor_id": {
          "description": "Logical synchronization anchor that this observation witnessed."
        },
        "timebase_id": {
          "description": "Clock on which observed_time_native was read. This is the observation clock, not necessarily the source or reference role it may take in a particular fit."
        },
        "external_event_id": {
          "description": "External event that supplied this marker reading, when one exists. Null for a manually identified timestamp; a trigger requires a cited event to be on the same clock."
        },
        "observed_time_native": {
          "description": "Observed anchor time in this observation's own native clock. It is preserved evidence and is never replaced with an aligned value."
        },
        "observation_role": {
          "description": "Declared role separating the primary reading, a retained replicate, and an excluded reading. An excluded observation cannot also claim inclusion in the fit."
        },
        "included_in_fit": {
          "description": "Whether this reading is selected to influence a fit (1) or retained without influencing it (0). Replicates and withheld observations remain evidence and are not pooled as independent anchors."
        },
        "evidence_class": {
          "description": "Whether the reading is device-level and identity-independent or depends on the identity of a biological event. Null means the class was never declared, not that it is device-level."
        },
        "uncertainty_s": {
          "description": "Recorded reading uncertainty for this observation, in seconds on its native clock. Null means no uncertainty was recorded, not zero; it is preserved and propagated but never used as a fit weight."
        },
        "uncertainty_semantics": {
          "description": "Text stating what the recorded uncertainty value means when one is present. Null when uncertainty_s is absent."
        },
        "source_file_id": {
          "description": "Registered source file from which this anchor reading came, when file-backed. Null when no source file is cited."
        },
        "mapping_profile_version_id": {
          "description": "Configuration-profile version used to interpret this observation, when one was registered. Null means no mapping-profile version is cited."
        },
        "source_locator": {
          "description": "Location of this observation within its source, such as a row or marker locator, when one was retained."
        },
        "notes": {
          "description": "Free-text audit log and remarks about this reading, including declared inclusion decisions. Never interpreted as a timestamp or fit result."
        }
      }
    },
    "alignment_anchor_identity_evidence": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "Records that an identity-dependent anchor observation is qualified by one existing track-to-entity identity association. Several rows may preserve ambiguity, and fitting never consumes their scores.",
      "columns": {
        "alignment_anchor_identity_evidence_id": {
          "description": "VAWLUME surrogate key for the evidence link, generated by the database."
        },
        "anchor_observation_id": {
          "description": "Identity-dependent anchor observation being qualified. A trigger refuses the link if the observation does not declare that evidence class."
        },
        "tracking_identity_association_id": {
          "description": "Existing native-track to canonical-entity association cited as identity evidence. It cannot be deleted while this qualifying link remains."
        },
        "notes": {
          "description": "Free-text remarks about why this identity association qualifies the observation. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_anchor_residuals": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One run-specific residual for a logical anchor, naming the exact source-clock and reference-clock observations compared. It is fit evidence left after prediction, not a validation verdict.",
      "columns": {
        "alignment_anchor_residual_id": {
          "description": "VAWLUME surrogate key for the residual row, generated by the database."
        },
        "alignment_run_id": {
          "description": "Source-to-reference transform attempt under which this residual was calculated."
        },
        "alignment_anchor_id": {
          "description": "Logical anchor whose two clock observations produced this run-specific residual."
        },
        "source_observation_id": {
          "description": "Anchor observation on the transform's source clock. A trigger distinguishes this endpoint from the reference observation and holds it within the same run and anchor."
        },
        "reference_observation_id": {
          "description": "Anchor observation on the transform's target reference clock. A trigger distinguishes this endpoint from the source observation and holds it within the same run and anchor."
        },
        "observed_source_time": {
          "description": "Native source-clock reading copied from the cited source observation when this residual was computed, retained so the calculation can be audited."
        },
        "observed_reference_time": {
          "description": "Native reference-clock reading copied from the cited reference observation when this residual was computed, retained so the calculation can be audited."
        },
        "predicted_reference_time": {
          "description": "Reference-clock time predicted by applying this run's stored transform to observed_source_time."
        },
        "residual_s": {
          "description": "Observed reference time minus predicted reference time, in seconds. The value is reported evidence and is never itself a threshold, pass/fail result, or calibrated quality judgement."
        },
        "included_in_fit": {
          "description": "Whether this anchor pair influenced the fitted coefficients (1) or was evaluated without influencing them (0). A withheld anchor can still carry a residual."
        },
        "exclusion_reason": {
          "description": "Reason this residual's observations were excluded from the fit. Required when included_in_fit is 0 and otherwise normally absent."
        },
        "notes": {
          "description": "Free-text remarks about this run-specific residual. Never parsed by VAWLUME."
        }
      }
    },
    "alignment_segments": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "One continuous affine segment of a stored source-to-reference transform, applying target_time = scale times source_time plus offset_s. These coefficients, together with native timestamps, are authoritative for projection.",
      "columns": {
        "alignment_segment_id": {
          "description": "VAWLUME surrogate key for the transform segment, generated by the database."
        },
        "alignment_run_id": {
          "description": "Source-to-reference transform attempt whose coefficients this segment carries."
        },
        "segment_index": {
          "description": "One-based order of the segment within the transform. Application validation requires the complete segment set to tile the source axis without gaps or overlaps."
        },
        "source_start": {
          "description": "Inclusive lower bound on the source clock for this segment. Null opens the first segment below all finite source times."
        },
        "source_end": {
          "description": "Exclusive upper bound on the source clock for this segment. Null opens the last segment above all finite source times."
        },
        "scale": {
          "description": "Multiplicative coefficient applied to a native source-clock value within this segment. Positive segment scales preserve increasing time."
        },
        "offset_s": {
          "description": "Additive coefficient in seconds applied after scale to express a source-clock value on the reference clock."
        },
        "rmse_s": {
          "description": "Root-mean-square residual in seconds for anchors assigned to this segment. Null when no segment summary was recorded; it reports fit evidence rather than acceptance."
        },
        "uncertainty_s": {
          "description": "Uncalibrated uncertainty bound in seconds on the reference clock, currently the largest recorded uncertainty among anchors determining the segment. Null means no contributing anchor recorded uncertainty, not zero."
        },
        "uncertainty_semantics": {
          "description": "Text declaring how uncertainty_s was derived. Required when the bound is present because this column could legitimately hold differently derived uncertainty quantities."
        }
      }
    },
    "aligned_external_events": {
      "kind": "table",
      "domain": "alignment_external",
      "description": "Optional regenerable cache of external-event intervals projected onto a transform's target clock. It is never the timing authority, no public refresh API currently populates it, and alignment does not require a row here.",
      "columns": {
        "aligned_external_event_id": {
          "description": "VAWLUME surrogate key for the cached projection, generated by the database."
        },
        "external_event_id": {
          "description": "Native external event whose time interval this cache projects without changing the event's stored native timestamps."
        },
        "alignment_run_id": {
          "description": "Stored source-to-reference transform from which this cached projection was derived."
        },
        "target_timebase_id": {
          "description": "Clock in which the cached aligned interval is expressed. It is expected to be the cited run's target reference clock."
        },
        "start_time_aligned_s": {
          "description": "Cached event start in seconds on the target clock, derived from the native start and stored coefficients rather than replacing the native value."
        },
        "end_time_aligned_s": {
          "description": "Cached event end in seconds on the target clock. Null for a point event; when present it cannot precede the aligned start."
        },
        "uncertainty_s": {
          "description": "Uncalibrated uncertainty bound in seconds inherited from the transform segment used for this cache. Null means no bound was recorded, not zero."
        },
        "uncertainty_semantics": {
          "description": "Text stating the semantics inherited by the cached uncertainty value. Null when uncertainty_s is absent."
        }
      }
    },
    "v_sequence_members": {
      "kind": "view",
      "domain": "views",
      "description": "Sequence membership joined to its owning sequence, so an ordered event list can be read together with the analysis run, recording, epoch, and scope that define it without writing the join by hand.",
      "columns": {
        "sequence_member_id": { "same_as": "sequence_members.sequence_member_id" },
        "sequence_id": { "same_as": "sequence_members.sequence_id" },
        "analysis_run_id": { "same_as": "sequences.analysis_run_id" },
        "recording_id": { "same_as": "sequences.recording_id" },
        "epoch_id": { "same_as": "sequences.epoch_id" },
        "scope_entity_id": { "same_as": "sequences.scope_entity_id" },
        "event_set_kind": { "same_as": "sequences.event_set_kind" },
        "ordinal_position": { "same_as": "sequence_members.ordinal_position" },
        "detection_id": { "same_as": "sequence_members.detection_id" },
        "consensus_event_id": { "same_as": "sequence_members.consensus_event_id" },
        "external_event_id": { "same_as": "sequence_members.external_event_id" },
        "aligned_external_event_id": { "same_as": "sequence_members.aligned_external_event_id" },
        "start_time_s": { "same_as": "sequence_members.start_time_s" },
        "end_time_s": { "same_as": "sequence_members.end_time_s" }
      }
    }
  },
  "relationships": [
    {
      "source_table": "config_profiles",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each configuration profile that names a project belongs to that project. A profile with no project is built in and applies to every project in the database."
    },
    {
      "source_table": "config_profile_versions",
      "source_columns": ["profile_id"],
      "target_table": "config_profiles",
      "target_columns": ["profile_id"],
      "description": "Each registered version is a version of exactly one configuration profile. A profile accumulates versions over time and none of them replaces another."
    },
    {
      "source_table": "project_profile_assignments",
      "source_columns": ["profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each project-level assignment names the exact profile version it assigns. The cited version cannot be deleted while the assignment stands."
    },
    {
      "source_table": "project_profile_assignments",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each project-level assignment names the project it applies to, and is removed with that project."
    },
    {
      "source_table": "source_files",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each registered source file was discovered within one project, and its location is unique within that project rather than globally."
    },
    {
      "source_table": "ingestion_runs",
      "source_columns": ["mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each ingestion attempt names the exact mapping profile version that interpreted its sources, which is what makes the rows it produced traceable to the rules that produced them."
    },
    {
      "source_table": "ingestion_runs",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each ingestion attempt is an attempt to ingest one project."
    },
    {
      "source_table": "ingestion_files",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each per-file outcome names the source file it reports on."
    },
    {
      "source_table": "ingestion_files",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each per-file outcome belongs to the ingestion attempt that produced it, so one file can have a different outcome in each attempt."
    },
    {
      "source_table": "entity_types",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each hierarchy level belongs to one project, because two projects may use the same native word for different levels."
    },
    {
      "source_table": "experimental_entities",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each entity records the ingestion attempt that first created it. Later compatible attempts leave this provenance alone and record their own audit rows instead."
    },
    {
      "source_table": "experimental_entities",
      "source_columns": ["entity_type_id"],
      "target_table": "entity_types",
      "target_columns": ["entity_type_id"],
      "description": "Each entity sits at exactly one level of its project's hierarchy, and its native identifier is unique only within that level."
    },
    {
      "source_table": "experimental_entities",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each entity belongs to one project."
    },
    {
      "source_table": "entity_relationships",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each relationship may cite the source file it was read from, when it came from a single file."
    },
    {
      "source_table": "entity_relationships",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each relationship records the ingestion attempt that first created it, when it arose from one."
    },
    {
      "source_table": "entity_relationships",
      "source_columns": ["child_entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Names the contained or subordinate endpoint of the relationship. This is the child side; the parent side is a separate reference into the same table, and the two may not name the same entity."
    },
    {
      "source_table": "entity_relationships",
      "source_columns": ["parent_entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Names the containing or superior endpoint of the relationship. This is the parent side, distinct from the child reference into the same table, and which endpoint is which is part of the relationship's meaning."
    },
    {
      "source_table": "entity_relationships",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each relationship belongs to the project both of its endpoints belong to."
    },
    {
      "source_table": "entity_attributes",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each attribute may cite the source file its value was read from."
    },
    {
      "source_table": "entity_attributes",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each attribute records the ingestion attempt that first created it, when it arose from one."
    },
    {
      "source_table": "entity_attributes",
      "source_columns": ["entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Each attribute describes exactly one entity and is removed with it."
    },
    {
      "source_table": "recordings",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each recording is anchored to exactly one registered source file, and each such file backs at most one recording. This one-to-one anchor is the recording's durable identity and the boundary extractor imports resolve against."
    },
    {
      "source_table": "recordings",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each recording belongs to one project."
    },
    {
      "source_table": "recording_channels",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Each channel belongs to exactly one recording, which is what makes anything attached to a channel specific to that session."
    },
    {
      "source_table": "recording_entity_links",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each participation record names the ingestion attempt that first created it, when it arose from one."
    },
    {
      "source_table": "recording_entity_links",
      "source_columns": ["entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Names the entity that took part in the recording."
    },
    {
      "source_table": "recording_entity_links",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the entity took part in. One entity may be linked to one recording more than once under different roles or intervals."
    },
    {
      "source_table": "recording_attributes",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each attribute may cite the source file its value was read from."
    },
    {
      "source_table": "recording_attributes",
      "source_columns": ["ingestion_run_id"],
      "target_table": "ingestion_runs",
      "target_columns": ["ingestion_run_id"],
      "description": "Each attribute records the ingestion attempt that first created it, when it arose from one."
    },
    {
      "source_table": "recording_attributes",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Each attribute describes exactly one recording and is removed with it."
    },
    {
      "source_table": "entity_profile_assignments",
      "source_columns": ["profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each entity-level assignment names the exact profile version it assigns. The cited version cannot be deleted while the assignment stands."
    },
    {
      "source_table": "entity_profile_assignments",
      "source_columns": ["entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Each entity-level assignment names the entity it applies to, and is removed with that entity."
    },
    {
      "source_table": "recording_profile_assignments",
      "source_columns": ["profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each recording-level assignment names the exact profile version it assigns. The cited version cannot be deleted while the assignment stands."
    },
    {
      "source_table": "recording_profile_assignments",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Each recording-level assignment names the recording it applies to, whether the recording was given it directly or inherited it."
    },
    {
      "source_table": "recording_epochs",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each interval may cite the source file it was imported from, and cites none when it was declared directly."
    },
    {
      "source_table": "recording_epochs",
      "source_columns": ["parent_epoch_id"],
      "target_table": "recording_epochs",
      "target_columns": ["epoch_id"],
      "description": "Names the enclosing interval when this one is nested inside another of the same recording. A top-level interval names none, so the column is what distinguishes nesting depth rather than a separate level column."
    },
    {
      "source_table": "recording_epochs",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Each interval falls within exactly one recording, and its bounds are seconds from that recording's start."
    },
    {
      "source_table": "coordinate_systems",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each declared frame belongs to one project and is shared reference data within it."
    },
    {
      "source_table": "channel_placements",
      "source_columns": ["source_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each placement may cite the device or setup profile version it was authored from. Provenance only: a reusable profile describes many sessions and does not itself assert where a microphone was during this one."
    },
    {
      "source_table": "channel_placements",
      "source_columns": ["coordinate_system_id"],
      "target_table": "coordinate_systems",
      "target_columns": ["coordinate_system_id"],
      "description": "Each placement expresses its coordinates in exactly one declared frame, which must belong to the same project as the channel. A frame cannot be deleted while a placement still cites it, because coordinates in no declared space would be worse than refusing the delete."
    },
    {
      "source_table": "channel_placements",
      "source_columns": ["recording_channel_id"],
      "target_table": "recording_channels",
      "target_columns": ["recording_channel_id"],
      "description": "Each placement positions exactly one channel's microphone, and a channel has at most one placement. A microphone moved mid-session is therefore unrepresentable rather than silently averaged."
    },
    {
      "source_table": "extractor_versions",
      "source_columns": ["extractor_id"],
      "target_table": "extractors",
      "target_columns": ["extractor_id"],
      "description": "Each registered version is a version of exactly one extractor tool."
    },
    {
      "source_table": "artifacts",
      "source_columns": ["parent_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact this one was derived from, such as the native container an export was written out of. An artifact with no recorded antecedent names none."
    },
    {
      "source_table": "artifacts",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Each artifact may cite the registered source file it is, when the same file was also discovered as project source material. An artifact registered only as extractor provenance cites none."
    },
    {
      "source_table": "artifacts",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each artifact is registered under one project, and its location is unique within that project rather than globally."
    },
    {
      "source_table": "extraction_runs",
      "source_columns": ["settings_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the profile version recording how the extractor was configured for this run. A run names none when its settings were not recoverable, which is an absence rather than a default."
    },
    {
      "source_table": "extraction_runs",
      "source_columns": ["output_mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the profile version that interpreted this run's export columns. A separate reference from the settings one, because how the output was read and how the tool was configured are different facts and are never conflated."
    },
    {
      "source_table": "extraction_runs",
      "source_columns": ["extractor_version_id"],
      "target_table": "extractor_versions",
      "target_columns": ["extractor_version_id"],
      "description": "Each run was produced by exactly one extractor version, and that version cannot be deleted while the run cites it."
    },
    {
      "source_table": "extraction_runs",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each run belongs to one project, within which its run key is unique."
    },
    {
      "source_table": "extraction_run_inputs",
      "source_columns": ["recording_channel_id"],
      "target_table": "recording_channels",
      "target_columns": ["recording_channel_id"],
      "description": "Names the particular channel consumed, when the run read one channel rather than the whole recording. A whole-recording input names none."
    },
    {
      "source_table": "extraction_run_inputs",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the run consumed. This declaration is what makes a detection on that recording admissible for the run."
    },
    {
      "source_table": "extraction_run_inputs",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Names the run that consumed this input."
    },
    {
      "source_table": "extraction_run_profiles",
      "source_columns": ["profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Each run-level assignment names the exact profile version it assigns. The cited version cannot be deleted while the assignment stands."
    },
    {
      "source_table": "extraction_run_profiles",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Each run-level assignment names the run it applies to, whether the run was given it directly or inherited it."
    },
    {
      "source_table": "extraction_run_artifacts",
      "source_columns": ["artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact the run cites in this role."
    },
    {
      "source_table": "extraction_run_artifacts",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Names the run the artifact belongs to."
    },
    {
      "source_table": "extractor_objects",
      "source_columns": ["source_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact this hierarchy node was read from. A node inferred from the run rather than from a particular file names none."
    },
    {
      "source_table": "extractor_objects",
      "source_columns": ["parent_object_id"],
      "target_table": "extractor_objects",
      "target_columns": ["extractor_object_id"],
      "description": "Names the enclosing node when this one is nested inside another from the same run. A top-level node names none, which is what makes the extractor's own nesting readable without a separate depth column."
    },
    {
      "source_table": "extractor_objects",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Each hierarchy node belongs to one run, because it is that run's own organization of its output rather than a shared structure."
    },
    {
      "source_table": "extractor_object_recordings",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names a recording the extractor-native node covers."
    },
    {
      "source_table": "extractor_object_recordings",
      "source_columns": ["extractor_object_id"],
      "target_table": "extractor_objects",
      "target_columns": ["extractor_object_id"],
      "description": "Names the extractor-native node being mapped. A node may cover several recordings, which is why this mapping is a table rather than a column."
    },
    {
      "source_table": "extractor_features",
      "source_columns": ["extractor_version_id"],
      "target_table": "extractor_versions",
      "target_columns": ["extractor_version_id"],
      "description": "Each declared feature belongs to one extractor version, because what a tool emits and what it means by it can change between versions."
    },
    {
      "source_table": "feature_mappings",
      "source_columns": ["mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the profile version that declared this correspondence, so the judgement is attributable to the document that made it. A mapping seeded with the repository rather than authored names none."
    },
    {
      "source_table": "feature_mappings",
      "source_columns": ["canonical_feature_id"],
      "target_table": "canonical_features",
      "target_columns": ["canonical_feature_id"],
      "description": "Names the canonical feature the extractor's feature is mapped to."
    },
    {
      "source_table": "feature_mappings",
      "source_columns": ["extractor_feature_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names the extractor-declared feature being mapped."
    },
    {
      "source_table": "feature_relationships",
      "source_columns": ["feature_b_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names the higher-identifier endpoint of the pairing. The two endpoint references into this one table are ordered by identifier so that a pairing cannot be stored twice in opposite directions; the position carries ordering rather than role."
    },
    {
      "source_table": "feature_relationships",
      "source_columns": ["feature_a_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names the lower-identifier endpoint of the pairing, and is the other of the two references into this one table. Which side is a reference for a comparison is recorded on the pairing itself, not by this position."
    },
    {
      "source_table": "detections",
      "source_columns": ["extractor_object_id"],
      "target_table": "extractor_objects",
      "target_columns": ["extractor_object_id"],
      "description": "Names the node of the extractor's own hierarchy this detection sits under. A detection from an export declaring no container hierarchy names none."
    },
    {
      "source_table": "detections",
      "source_columns": ["source_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact this detection was read from, and forms part of the detection's uniqueness within its run so that one run reading two exports keeps their events distinct."
    },
    {
      "source_table": "detections",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the event was reported in. That recording must already be a declared input to the detection's run, which a trigger enforces."
    },
    {
      "source_table": "detections",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Names the run that reported this detection. Reading two runs' detections together is a matching operation and never a simple union of these rows."
    },
    {
      "source_table": "event_measurements",
      "source_columns": ["source_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact this value was read from."
    },
    {
      "source_table": "event_measurements",
      "source_columns": ["canonical_feature_id"],
      "target_table": "canonical_features",
      "target_columns": ["canonical_feature_id"],
      "description": "Names the canonical feature this value was read as. A measurement of a quantity with no canonical counterpart names none, and remains a preserved native value rather than becoming an error."
    },
    {
      "source_table": "event_measurements",
      "source_columns": ["extractor_feature_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names the extractor-declared feature this is a measurement of, which is what makes the stored number interpretable. Required, and the feature cannot be deleted while measurements cite it."
    },
    {
      "source_table": "event_measurements",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection this measurement describes. One detection carries many measurements, and may carry more than one of nominally the same quantity under different operational variants."
    },
    {
      "source_table": "unmapped_source_values",
      "source_columns": ["mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the profile version in force when this value went unclaimed, so a later revision can be checked against what it previously failed to describe."
    },
    {
      "source_table": "unmapped_source_values",
      "source_columns": ["source_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact the unclaimed value was read from."
    },
    {
      "source_table": "unmapped_source_values",
      "source_columns": ["extractor_object_id"],
      "target_table": "extractor_objects",
      "target_columns": ["extractor_object_id"],
      "description": "Scopes the unclaimed value to a node of the extractor's hierarchy, when it appeared at container level. One of several alternative scopes, and null when the value belongs to a different one."
    },
    {
      "source_table": "unmapped_source_values",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Scopes the unclaimed value to one detection, when it appeared on an event row. The narrowest of the alternative scopes, and the usual one for an unrecognized column in a per-event export."
    },
    {
      "source_table": "unmapped_source_values",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Scopes the unclaimed value to a run, when it belongs to the output as a whole rather than to any one event or container."
    },
    {
      "source_table": "curation_events",
      "source_columns": ["source_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact the review state was read from."
    },
    {
      "source_table": "curation_events",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection the review act concerns. A detection may accumulate several review acts, and none of them removes it."
    },
    {
      "source_table": "classification_runs",
      "source_columns": ["settings_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the profile version recording how the labelling was configured, when that was recoverable."
    },
    {
      "source_table": "classification_runs",
      "source_columns": ["model_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the registered model that produced the labels, when one was supplied. A labelling whose source states labels without saying what produced them names none, and no model is inferred to fill the gap."
    },
    {
      "source_table": "classification_runs",
      "source_columns": ["extractor_object_id"],
      "target_table": "extractor_objects",
      "target_columns": ["extractor_object_id"],
      "description": "Scopes the labelling to a node of the extractor's hierarchy, when it covered less than the whole run."
    },
    {
      "source_table": "classification_runs",
      "source_columns": ["parent_extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Names the extraction run whose detections are being labelled. An assignment may only concern a detection from this run, which a trigger enforces."
    },
    {
      "source_table": "classification_runs",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each labelling belongs to one project."
    },
    {
      "source_table": "classification_classes",
      "source_columns": ["classification_run_id"],
      "target_table": "classification_runs",
      "target_columns": ["classification_run_id"],
      "description": "Each class belongs to one labelling, because the same word may mean different things in two labellings and a shared class table would assert otherwise."
    },
    {
      "source_table": "classification_assignments",
      "source_columns": ["classification_class_id"],
      "target_table": "classification_classes",
      "target_columns": ["classification_class_id"],
      "description": "Names the class assigned, which must be one of the classes belonging to this same labelling; a trigger enforces that."
    },
    {
      "source_table": "classification_assignments",
      "source_columns": ["classification_run_id"],
      "target_table": "classification_runs",
      "target_columns": ["classification_run_id"],
      "description": "Names the labelling that made this assignment. One detection may be assigned a class by several labellings, but at most once by each."
    },
    {
      "source_table": "classification_assignments",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection being labelled, which must belong to the labelling's parent extraction run; a trigger enforces that."
    },
    {
      "source_table": "analysis_runs",
      "source_columns": ["parent_analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis this one is a child of, for a derivation with a single genuine parent. A run composed from several sources names none here and records its lineage in the sources relation instead, so this column is never overloaded to mean one of several."
    },
    {
      "source_table": "analysis_runs",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Each analysis belongs to one project, within which its run key is unique."
    },
    {
      "source_table": "analysis_run_profiles",
      "source_columns": ["profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the exact profile version that governed the analysis in this role. The cited version cannot be deleted while the analysis stands, because a conclusion whose rules had vanished would be unreproducible."
    },
    {
      "source_table": "analysis_run_profiles",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis the profile version governed."
    },
    {
      "source_table": "analysis_run_extraction_inputs",
      "source_columns": ["extraction_run_id"],
      "target_table": "extraction_runs",
      "target_columns": ["extraction_run_id"],
      "description": "Names an extraction run the analysis was permitted to draw detections from. This declaration is what bounds the analysis's population, and a group member from an undeclared run is refused."
    },
    {
      "source_table": "analysis_run_extraction_inputs",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis consuming this extraction run."
    },
    {
      "source_table": "analysis_run_sources",
      "source_columns": ["source_analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis that was consumed. One of two references into this same table, and the one that cannot be deleted while the derived analysis stands."
    },
    {
      "source_table": "analysis_run_sources",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the derived analysis that did the consuming. The other of the two references into this table, and never the same analysis as the source; both must belong to one project."
    },
    {
      "source_table": "candidate_pairs",
      "source_columns": ["detection_b_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the higher-identifier endpoint of the considered pair. The two endpoint references into this one table are ordered by identifier so a pair cannot be stored twice in opposite directions; the position carries ordering rather than extractor or role."
    },
    {
      "source_table": "candidate_pairs",
      "source_columns": ["detection_a_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the lower-identifier endpoint, and is the other of the two references into this table. The two detections must come from distinct extraction runs and from the pair's stated recording, which a trigger enforces."
    },
    {
      "source_table": "candidate_pairs",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording both endpoints belong to."
    },
    {
      "source_table": "candidate_pairs",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis that considered this pair. Two analyses may consider the same two detections and reach different conclusions, which is why the pair is unique within its analysis rather than globally."
    },
    {
      "source_table": "match_groups",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording whose detections this group covers."
    },
    {
      "source_table": "match_groups",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis that formed this group. That analysis's groups partition every detection in its declared inputs, so a detection missing from all of them would be a defect rather than an omission."
    },
    {
      "source_table": "match_group_members",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection that belongs to the group. It must come from the analysis's declared inputs and belong to no other group of that analysis; triggers enforce both."
    },
    {
      "source_table": "match_group_members",
      "source_columns": ["match_group_id"],
      "target_table": "match_groups",
      "target_columns": ["match_group_id"],
      "description": "Names the group the detection belongs to."
    },
    {
      "source_table": "consensus_events",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the derived interval falls in."
    },
    {
      "source_table": "consensus_events",
      "source_columns": ["match_group_id"],
      "target_table": "match_groups",
      "target_columns": ["match_group_id"],
      "description": "Names the group the interval was derived from, whose membership it repeats exactly. A derived interval whose group is removed keeps its value and loses its lineage rather than being deleted with it."
    },
    {
      "source_table": "consensus_events",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis that derived this interval."
    },
    {
      "source_table": "consensus_event_members",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names a detection the derived interval was computed from."
    },
    {
      "source_table": "consensus_event_members",
      "source_columns": ["consensus_event_id"],
      "target_table": "consensus_events",
      "target_columns": ["consensus_event_id"],
      "description": "Names the derived interval the detection contributed to."
    },
    {
      "source_table": "consilience_assessments",
      "source_columns": ["consensus_event_id"],
      "target_table": "consensus_events",
      "target_columns": ["consensus_event_id"],
      "description": "Names the derived interval being assessed, when the assessment targets one. Exactly one of this and the match-group reference is populated, so an assessment always has a single unambiguous subject."
    },
    {
      "source_table": "consilience_assessments",
      "source_columns": ["match_group_id"],
      "target_table": "match_groups",
      "target_columns": ["match_group_id"],
      "description": "Names the group being assessed, when the assessment targets one. The alternative to the consensus-event reference, never populated alongside it."
    },
    {
      "source_table": "consilience_assessments",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis that produced the assessment, and through which the rule set that defined the status can be recovered."
    },
    {
      "source_table": "manual_reviews",
      "source_columns": ["consensus_event_id"],
      "target_table": "consensus_events",
      "target_columns": ["consensus_event_id"],
      "description": "Names the derived interval the reviewer judged, when the review targets one. One of three alternative subjects, of which exactly one is populated."
    },
    {
      "source_table": "manual_reviews",
      "source_columns": ["match_group_id"],
      "target_table": "match_groups",
      "target_columns": ["match_group_id"],
      "description": "Names the correspondence the reviewer judged, when the review targets a group rather than a single detection or a derived interval."
    },
    {
      "source_table": "manual_reviews",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection the reviewer judged, when the review targets one. The narrowest of the three alternative subjects."
    },
    {
      "source_table": "manual_reviews",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis the review was made in the context of. A deleted analysis leaves the review in place without it, because a human judgement outlives the run that prompted it."
    },
    {
      "source_table": "manual_reference_events",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the reviewer marked the event in. The only reference this table carries: a reference event belongs to a recording and a named set, never to an analysis, so one reviewed subset can anchor comparisons across many configurations."
    },
    {
      "source_table": "agreement_statistics",
      "source_columns": ["scope_entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Names the experimental entity the aggregate was computed within, when it was scoped more narrowly than the whole analysis."
    },
    {
      "source_table": "agreement_statistics",
      "source_columns": ["feature_b_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names one endpoint of the registered feature pair a feature-level aggregate concerns. One of two references into this table, populated together with the other so the aggregate stays traceable to the exact pair it summarizes."
    },
    {
      "source_table": "agreement_statistics",
      "source_columns": ["feature_a_id"],
      "target_table": "extractor_features",
      "target_columns": ["extractor_feature_id"],
      "description": "Names the other endpoint of that registered feature pair. Unlike the endpoints of a candidate pair or a feature relationship, these two carry no enforced ordering, so neither position implies which extractor a side came from."
    },
    {
      "source_table": "agreement_statistics",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis the aggregate belongs to, ordinarily a child run of the matching analysis it summarizes."
    },
    {
      "source_table": "agreement_groups",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording the composed component covers, which must belong to the agreement run's project."
    },
    {
      "source_table": "agreement_groups",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the agreement run that composed this component. A trigger refuses a run that is not of the arbitrary-N agreement type, so a composed component cannot be attached to a pairwise matching analysis."
    },
    {
      "source_table": "agreement_group_members",
      "source_columns": ["detection_id"],
      "target_table": "detections",
      "target_columns": ["detection_id"],
      "description": "Names the detection belonging to the composed component. It cannot be deleted while the component holds it, which protects the member that no candidate edge joins and that would otherwise disappear without trace."
    },
    {
      "source_table": "agreement_group_members",
      "source_columns": ["agreement_group_id"],
      "target_table": "agreement_groups",
      "target_columns": ["agreement_group_id"],
      "description": "Names the composed component the detection belongs to."
    },
    {
      "source_table": "agreement_supporting_edges",
      "source_columns": ["candidate_pair_id"],
      "target_table": "candidate_pairs",
      "target_columns": ["candidate_pair_id"],
      "description": "Names the exact pairwise candidate edge being cited as support. It must come from a declared source analysis, describe the component's recording, and join two of its members. The cited candidate cannot be deleted while the citation stands, so a composed result never outlives its evidence."
    },
    {
      "source_table": "agreement_supporting_edges",
      "source_columns": ["agreement_group_id"],
      "target_table": "agreement_groups",
      "target_columns": ["agreement_group_id"],
      "description": "Names the composed component the cited edge helps hold together."
    },
    {
      "source_table": "timebases",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording this clock belongs to when it is recording-scoped. Null leaves the clock project-scoped rather than attaching it to a recording."
    },
    {
      "source_table": "timebases",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Names the project in which this clock is declared, whether the clock is project-scoped or also belongs to a recording."
    },
    {
      "source_table": "external_streams",
      "source_columns": ["timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the clock whose native coordinates are used by this stream's events and coverage intervals."
    },
    {
      "source_table": "external_streams",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording this logical stream accompanies when it is recording-scoped. Null leaves the stream scoped only to its project."
    },
    {
      "source_table": "external_streams",
      "source_columns": ["project_id"],
      "target_table": "projects",
      "target_columns": ["project_id"],
      "description": "Names the project that owns this logical external stream."
    },
    {
      "source_table": "external_stream_sources",
      "source_columns": ["mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the configuration-profile version used to interpret this source when one was registered; deletion of that version leaves the provenance row without the optional link."
    },
    {
      "source_table": "external_stream_sources",
      "source_columns": ["artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact that supplied these stream records when the provenance row is artifact-backed. Exactly one of this and the source-file reference is present."
    },
    {
      "source_table": "external_stream_sources",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Names the source file that supplied these stream records when the provenance row is file-backed. Exactly one of this and the artifact reference is present."
    },
    {
      "source_table": "external_stream_sources",
      "source_columns": ["external_stream_id"],
      "target_table": "external_streams",
      "target_columns": ["external_stream_id"],
      "description": "Names the logical external stream whose records came from this cited file or artifact."
    },
    {
      "source_table": "external_events",
      "source_columns": ["entity_id"],
      "target_table": "experimental_entities",
      "target_columns": ["entity_id"],
      "description": "Names the existing experimental entity that the source declared this event concerned, when that label resolved. This optional lookup is not visual-identity evidence."
    },
    {
      "source_table": "external_events",
      "source_columns": ["external_stream_id"],
      "target_table": "external_streams",
      "target_columns": ["external_stream_id"],
      "description": "Names the logical stream in which this event occurred and thereby identifies the event's native clock."
    },
    {
      "source_table": "external_event_attributes",
      "source_columns": ["external_event_id"],
      "target_table": "external_events",
      "target_columns": ["external_event_id"],
      "description": "Names the external event that this mapped long-form attribute describes."
    },
    {
      "source_table": "external_stream_coverage",
      "source_columns": ["external_stream_id"],
      "target_table": "external_streams",
      "target_columns": ["external_stream_id"],
      "description": "Names the logical stream whose observation coverage this native-clock interval asserts."
    },
    {
      "source_table": "alignment_sets",
      "source_columns": ["manifest_artifact_id"],
      "target_table": "artifacts",
      "target_columns": ["artifact_id"],
      "description": "Names the artifact carrying this set's session-specific manifest when artifact-backed. At most one of the artifact and source-file manifest references is present."
    },
    {
      "source_table": "alignment_sets",
      "source_columns": ["manifest_source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Names the source file carrying this set's session-specific manifest when file-backed. At most one of the source-file and artifact manifest references is present."
    },
    {
      "source_table": "alignment_sets",
      "source_columns": ["reference_timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the clock chosen as this alignment set's reference coordinate system, without asserting that its device is intrinsically more accurate."
    },
    {
      "source_table": "alignment_sets",
      "source_columns": ["recording_id"],
      "target_table": "recordings",
      "target_columns": ["recording_id"],
      "description": "Names the recording whose participating clocks this set aligns when the operation is recording-scoped."
    },
    {
      "source_table": "alignment_sets",
      "source_columns": ["analysis_run_id"],
      "target_table": "analysis_runs",
      "target_columns": ["analysis_run_id"],
      "description": "Names the analysis run that owns this user-facing alignment operation and records its caller-asserted software provenance. The unique reference makes the set the run's alignment result."
    },
    {
      "source_table": "time_alignment_runs",
      "source_columns": ["target_timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the target reference clock in which this transform expresses values. A trigger requires it to equal the parent set's reference clock."
    },
    {
      "source_table": "time_alignment_runs",
      "source_columns": ["source_timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the source clock whose native coordinates this transform accepts, distinct from the target reference clock."
    },
    {
      "source_table": "time_alignment_runs",
      "source_columns": ["alignment_set_id"],
      "target_table": "alignment_sets",
      "target_columns": ["alignment_set_id"],
      "description": "Names the user-facing alignment set that owns this one source-to-reference transform attempt."
    },
    {
      "source_table": "alignment_run_breakpoints",
      "source_columns": ["alignment_run_id"],
      "target_table": "time_alignment_runs",
      "target_columns": ["alignment_run_id"],
      "description": "Names the piecewise-affine transform attempt whose source clock carries this caller-declared breakpoint."
    },
    {
      "source_table": "alignment_anchors",
      "source_columns": ["alignment_set_id"],
      "target_table": "alignment_sets",
      "target_columns": ["alignment_set_id"],
      "description": "Names the alignment set within which this logical synchronization occurrence is identified and observed."
    },
    {
      "source_table": "alignment_anchor_observations",
      "source_columns": ["mapping_profile_version_id"],
      "target_table": "config_profile_versions",
      "target_columns": ["profile_version_id"],
      "description": "Names the configuration-profile version used to interpret this clock reading when one was registered; deletion leaves the observation without the optional profile link."
    },
    {
      "source_table": "alignment_anchor_observations",
      "source_columns": ["source_file_id"],
      "target_table": "source_files",
      "target_columns": ["source_file_id"],
      "description": "Names the registered source file from which this anchor reading came, when the observation is file-backed."
    },
    {
      "source_table": "alignment_anchor_observations",
      "source_columns": ["external_event_id"],
      "target_table": "external_events",
      "target_columns": ["external_event_id"],
      "description": "Names the external event that supplied this anchor reading when one exists. A trigger requires the event's stream to use the same clock as the observation."
    },
    {
      "source_table": "alignment_anchor_observations",
      "source_columns": ["timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the clock on which this observation read the logical anchor, independently of whether that clock is a source or reference in a particular transform."
    },
    {
      "source_table": "alignment_anchor_observations",
      "source_columns": ["alignment_anchor_id"],
      "target_table": "alignment_anchors",
      "target_columns": ["alignment_anchor_id"],
      "description": "Names the logical synchronization anchor that this one native-clock reading witnessed."
    },
    {
      "source_table": "alignment_anchor_identity_evidence",
      "source_columns": ["tracking_identity_association_id"],
      "target_table": "tracking_identity_associations",
      "target_columns": ["tracking_identity_association_id"],
      "description": "Names the existing track-to-entity association cited as identity evidence for the anchor reading. Deletion is restricted so admitted evidence cannot disappear silently."
    },
    {
      "source_table": "alignment_anchor_identity_evidence",
      "source_columns": ["anchor_observation_id"],
      "target_table": "alignment_anchor_observations",
      "target_columns": ["anchor_observation_id"],
      "description": "Names the identity-dependent anchor observation qualified by this evidence link; a trigger refuses links to undeclared or device-level observations."
    },
    {
      "source_table": "alignment_anchor_residuals",
      "source_columns": ["reference_observation_id"],
      "target_table": "alignment_anchor_observations",
      "target_columns": ["anchor_observation_id"],
      "description": "Names the observation on the transform's target reference clock. A trigger keeps this endpoint distinct from and correctly oriented against the source observation."
    },
    {
      "source_table": "alignment_anchor_residuals",
      "source_columns": ["source_observation_id"],
      "target_table": "alignment_anchor_observations",
      "target_columns": ["anchor_observation_id"],
      "description": "Names the observation on the transform's source clock. A trigger keeps this endpoint distinct from and correctly oriented against the reference observation."
    },
    {
      "source_table": "alignment_anchor_residuals",
      "source_columns": ["alignment_anchor_id"],
      "target_table": "alignment_anchors",
      "target_columns": ["alignment_anchor_id"],
      "description": "Names the logical anchor whose source and reference observations produced this residual under the cited transform."
    },
    {
      "source_table": "alignment_anchor_residuals",
      "source_columns": ["alignment_run_id"],
      "target_table": "time_alignment_runs",
      "target_columns": ["alignment_run_id"],
      "description": "Names the source-to-reference transform attempt under which this anchor's prediction and residual were calculated."
    },
    {
      "source_table": "alignment_segments",
      "source_columns": ["alignment_run_id"],
      "target_table": "time_alignment_runs",
      "target_columns": ["alignment_run_id"],
      "description": "Names the source-to-reference transform attempt whose authoritative coefficients this segment carries."
    },
    {
      "source_table": "aligned_external_events",
      "source_columns": ["target_timebase_id"],
      "target_table": "timebases",
      "target_columns": ["timebase_id"],
      "description": "Names the clock in which this optional cached event interval is expressed. The transform and native timestamp remain authoritative."
    },
    {
      "source_table": "aligned_external_events",
      "source_columns": ["alignment_run_id"],
      "target_table": "time_alignment_runs",
      "target_columns": ["alignment_run_id"],
      "description": "Names the stored source-to-reference transform from which this optional cached interval was derived."
    },
    {
      "source_table": "aligned_external_events",
      "source_columns": ["external_event_id"],
      "target_table": "external_events",
      "target_columns": ["external_event_id"],
      "description": "Names the native external event whose interval this cache projects without replacing or updating its native timestamps."
    }
  ]
}
