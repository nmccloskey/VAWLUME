function keys = supportedTransformKeys()
%SUPPORTEDTRANSFORMKEYS Whitelisted executable source_mapping transforms.
keys = [
    "identity"
    "kHz_to_Hz"
    "kHz_per_s_to_Hz_per_s"
    "ms_to_s"
    ];
end
